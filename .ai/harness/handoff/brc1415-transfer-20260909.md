# BRC14/BRC15 接手 Handoff — 2026-09-09

## 交接结论

用户要求转交其他执行者，本执行者停止实施。本次只写交接，不启动模型、不修改产品代码、不合并 PR。

**BRC14/BRC15 尚未通过。已完成真实正向 admission、authoring/challenge/adoption；未完成真实 worker → verifier → 修复 PR → 人工 merge → 自动收尾 → 组末 fresh audit。** 不得把前置通过写成最终通过。

最新直接阻断是 PR #374 的 CI：main 已采用 `gpt-6-astra`，三处测试仍用 `gpt-5.6-terra` 作为正确身份预期。不是要求用户切回旧模型。接手第一刀是修正这些过期 positive fixtures，保留 negative identity mismatch 测试。

## 本次交接已实时回读

- PR https://github.com/Ancienttwo/repo-harness/pull/374：OPEN，head `88d6ae8ee697e83a3ca0367010134a60a06f7584`，未 merge。
- CI https://github.com/Ancienttwo/repo-harness/actions/runs/34325936636：Test 和 Required / CI FAILED；Ubuntu/macOS/Windows 三个 MCP path matrix SUCCESS。
- 修复 worktree `/Users/ancienttwo/Projects/repo-harness-wt-fleet-packaged-helper`：tracked/untracked Git status clean。
- 目标 `/Users/ancienttwo/Projects/byok-brc1415-canary`：Git status clean，HEAD `294be4e7bcd5f2b8d7535f5da7fb4ee7fd8e6839`。
- 本文其余运行细节来自已保存的任务证据；使用时核对精确输入，不假定授权、token、CI 或 main 未变化。

## 用户固定范围与约束

- 按 Phase A 收口：一组、两个 slots，max_parallel_tasks=2 是容量上限，可串行，manual merge。
- 原始 Issues 为 Ancienttwo/byok-sdk #177 / #178，不重抽十项，不造缺陷，不由本地代写模型 Issue。
- 至少一项真实 bugfix/test-gap 通过实际 Docker worker/verifier、PR、人工 merge 和自动收尾。
- 组末同一次新会话 fresh audit 同时用于 BRC14/BRC15；组间 1→2→3→terminal 用 model-free 回归证明，不声称真实跑了三组。
- 文本激活必须是 **`@github connector`**；放弃 UI 视角/app 预选。浏览器使用用户当前默认模型及 thinking，不注入覆盖。
- 不重开 Docker 基建审查、旧十项 shadow、Canary 4/5、auto-merge、refactor 扩展、GC 或 package release。
- 保留 BRC15a 负面结果及 #354/#342/#346/#363/#366 已有结论；默认产品 off。
- 不复活 stopped grant，不加预算绕失败，不对未知结果换 key 重发；先 reconcile 原请求。
- `adopt --dry-run` 可能 challenge、读取、seal，不是免费 preflight。
- 用户已多次批准推进；不要因交接重新要求审批所有既有步骤。最后停点曾按 AGENTS 的“第二个范围外故障 hard stop”记录；本次新请求仅授权交接，未实施测试修复。
- 不读全库、全部 Issue 或长对话。优先本文列出的准确文件。不要碰其他 pane 的 Oracle 改动和共享 checkout WIP。

## 工作包 A：PR #374 已做什么

Branch `codex/fleet-packaged-helper`，worktree `/Users/ancienttwo/Projects/repo-harness-wt-fleet-packaged-helper`，base `223031342d2f54a886dc5adad0506dbcf07f2bcc`。

根因：fleet acquire 的 defaultStart/defaultProject 还调用 target 内的 `scripts/contract-worktree.sh` / `scripts/plan-to-todo.sh`，而真实安装目标只有 packaged helpers，导致 provision_failed，模型成本已花但 worker 无法启动。

改动：`src/effects/fleet/acquire.ts` 通过既有 `runHelper({ trustedPackage: true })` 调两个 helper；保持原参数、cwd、错误传播和 claim 补偿。helper runner/platform 从 `src/cli/runtime/` 移到 `src/effects/runtime/`，消费者直接改 import，无兼容 shim。更新一处 capability source path 和 deterministic architecture projections。没有改 Oracle、Docker、admission、预算或模型配置。

真实 CLI 回归不再把 helper 复制进 target：验证 package-only 目标可建 worktree、绑定 lease/token、拒绝重复 acquire。首次 red、修复后 green；层依赖检查发现 CLI 反向依赖后通过上述 relocation 修正。

本地最终验收：10 个 executable checks、21 个 contract checks PASS；包含定向 fleet/runtime 测试、typecheck 和仓库必需 integrity checks。未用本地 green 冒充 CI green。

- Plan/contract/review/notes stem：`20260909-1526-fleet-packaged-helper`
- durable research：`docs/researches/20260909-fleet-packaged-helper.md`
- run：`run-20260909T154531-2163-20260909-1526-fleet-packaged-helper`
- receipt：`/tmp/brc-fleet-helper-receipt.json`
- 最终日志：`/tmp/brc-fleet-helper-acceptance.log`、`/tmp/brc-fleet-helper-finalize.log`
- 前置失败证据：`tasks/evidence/fleet-packaged-helper-pre-fix.log`
- typed receipt 必须消费 `.ai/harness/checks/latest.json` materialized report；不要改用 raw run snapshot，更不要改证据。

### CI 失败已在未修改 main 复现

三文件、五失败：

1. `tests/bootstrap-files.test.ts`：fleet subagent TOML required keys/model，约 line 94。
2. `tests/check-agent-tooling.test.ts`：real SubagentStart evidence pointer 正例。
3. `tests/subagent-handler.test.ts`：三个 composed native-child / verified routing 正例，约 line 921 的旧 model fixture 与实际 gatekeeper TOML 不符。

不要全局 replace 旧模型字符串；有些故意用于错误身份负例。当前配置才是预期 authority，修正正例，保留拒绝能力。

在 clean detached main `223031342d2f54a886dc5adad0506dbcf07f2bcc` 安装冻结依赖后执行：

```bash
bun test --timeout 60000 tests/bootstrap-files.test.ts tests/check-agent-tooling.test.ts tests/subagent-handler.test.ts
```

结果 60 pass / 5 fail，与 CI 一致。该临时 baseline worktree 已干净删除，无需重跑根因证明。

证据：修复 worktree 的 `.ai/harness/runs/brc374-main-baseline-failures.log`；`/tmp/brc374-baseline-failures.log`；`/tmp/brc-fleet-helper-ci-failed.log`。PR body 已包含此结论。三文件尚未改。

## 工作包 B：真实 canary 的保留现场

目标 worktree `/Users/ancienttwo/Projects/byok-brc1415-canary`，remote Ancienttwo/byok-sdk，branch `codex/brc1415-canary`。tracked report：`docs/researches/20260909-brc1415-delivery-canary.md`；完整本地历史指针：`.canary-scratch/delivery-handoff.md`。

最近 campaign `byok-brc1415-20260909-delivery` **STOPPED**：

- grant `83f83d442f01355e4d7f8dc6ff778214497154a4984ea135ac971a50fdd9358d`
- 冻结初始 target `7487d329de893129ed1ce97b3006db7525d71e11`
- 当时 harness PR #373 candidate `dc8c9eedbbc4e3e03187b1a400ca6fd7afe1caba`；该 PR 已合入，不能假装 #374 证据属于旧版本。
- intent `sha256:548bc0a50d7f26dddab39789fcbfd7aa583f376a5baf6cd5ea12a0e63911d347`
- author session `chgpt_20260909_151113_byok-brc1415-20260909-delivery-group-1-issue-aut`
- author conversation https://chatgpt.com/c/6aa105c3-138c-83ea-af2c-378df959e541
- author 一次，slot02 exact edit 一次，challenge passed，两条原 Issue 已 adoption。
- adoption receipt `sha256:2ec9aee590f049be10ae4c3b0b1472d5f59b2a59a86da7f3899ce2b450bb7372`
- publication `b4d260a0bcbe1aa87395a4a831a87166fff7c631`
- manifest `tasks/campaigns/byok-brc1415-20260909-delivery/group-1.issues.json`
- 177 plan/contract `20260909-1520-brc177-delivery`，planning publication `d5fc2daf27c0f884cb4972aa2dc9790d113417bb`
- 实际返回 `plan_ready / execution_ready / stable / blockers=[]`，随后 acquire provision_failed，原因就是 #374 修复的 helper 路径。
- 已 reconcile 原失败为 observed/no_progress，lease available、record null；最终 9 turns / 8 provider calls / 0 successful acquisitions / open reservations []。没有留下运行 worker。

关键文件均在 `.canary-scratch/`：`delivery-revision.log`、`delivery-adoption-result.json`、`delivery-plan-01.json`、`delivery-admitted-01.json`、`delivery-acquired-01.json`、`delivery-stopped.json`、`reconcile-delivery-acquire.ts`。`delivery-author-result.json` 很大，只 parse 必要字段，不输出整段对话。

### 两个 slots 的真实状态

- #177：README namespace count/import 的真实问题，尚未修复。`packages/sdk/src/readme.test.ts` 前置回归 2 fail / 1 pass，证据 `tasks/evidence/brc177-pre-fix.log`。test hash `sha256:e85f9ed54456ccf0043adddbe098a5da44671ec31d65284ac4f6ea9933262697`；log hash `sha256:0e288cf264b7e26690c6f19b630495ad834870bc37e4a7215c8ae5f4ab33fb92`。允许改动只限 README 和该测试，实际修复须通过 Docker worker。contract 已有 7 项 SDK 检查；不要另跑整个 harness suite。
- #178：已证伪。原有 packed-install smoke 已检查 umbrella entrypoint、7 namespaces 和 keys exclusion。证据 `.canary-scratch/delivery178-source-falsifier.log` 是 source inspection，不是 smoke execution。尚需既有 not_reproducible/close-not-planned 正式路径、typed decision 与 acceptance receipt，不能直接把它算完成。

### 下一次 continuation 仅准备，未 mint/start

- `packaged-resume-source.json`、`packaged-preflight.json` 已生成，绑定 stopped delivery source、target `294be4e...`。
- `packaged-helper-preflight.json` 已在真实目标用 `trustedPackage=true` 调 status/help，两者成功，无模型。普通 resolveHelper 可能受 REPO_HARNESS_SOURCE_ROOT 影响，不代表 production trustedPackage 路径。
- `make-packaged-grant.ts`、`start-packaged.ts` **未运行**。
- 拟用 campaign `byok-brc1415-20260909-packaged-delivery`；脚本仍硬绑定 PR374 merged + **旧失败 CI 34325936636 success**，当前应拒绝。必须在修复最终 head/CI 后更新精确版本/CI绑定，不能直接运行。
- 准备预算仍为 40 provider / 46 runner / 48 turns / 2 hours；不自动扩额。冻结时间必须在真正执行时生成，所有 stop/expiry 规则保留。
- 不从更早 stopped campaign 建 sibling continuation；原链为 transport → text-connector → delivery，continuation claim 已有绑定。

## 运行环境与敏感信息边界

- 已验证使用的 Oracle fork：`/Users/ancienttwo/Projects/oracle-wt-fork-0.20.0/dist/bin/oracle-cli.js`，source `9db96a10380ffcdb47b353896c9c1e3d5645bc2d`，version 0.20.0。通过 `REPO_HARNESS_ORACLE_BIN` 绑定。别用 PATH 上缺 evidence flags 的 upstream build；不改 Oracle 文件。重建 dist 可能需 chmod +x。
- 固定 Docker image：`sha256:72270cb098680b4e5e9e34f3ed7da6d861551bf946813a485069554055e08523`，env `BRC_CAMPAIGN_IMAGE`。未变部分精确比较后复用证据，不从零审查。
- 浏览器 Chrome Profile 13，current model，不覆盖 thinking。实际 Connector activation `@github connector`。
- Engineer `engineer:capability.sdk.sdk-root`，binding `54ad4d8a-9eb4-424f-ba11-127a4d30a89d` gen1；parent session `01a080bd-9b0c-7392-9573-f80818ca6675` 是旧执行者身份，新执行者须核对协议可否接续，不伪装旧身份。
- 旧 OAuth authorization `368611e4-dc72-403a-be63-dff450166214` 可能过期；loopback server 18765 的状态须 readback。现有 `delivery-enroll-oauth.ts` 走真实 PKCE。`.canary-scratch/delivery-engineer-oauth-private.json` 是 0600 凭证，**不要打印或写进 handoff**；不得合成 authorization。
- 目标 routing 已修正并冻结：base branch `codex/brc1415-canary`，review base `origin/codex/brc1415-canary`，merge target `refs/heads/codex/brc1415-canary`；active Sprint `plans/sprints/brc1415-canary.sprint.md`。
- protection fingerprint `sha256:97c53217c3c59abc03b3a46f07be0bb1a6179a635f5044d337b8323831ff6d0f`。授权后不要再改 routing/protected inputs。

## 接手顺序与完成标准

1. 只读核对 PR374/head、工作树 ownership 和上述三处测试。最小修正 positive fixtures；不切换用户模型，不重跑模型实验。运行三文件定向测试及 required integrity checks，按最终候选更新验收证据，获得 Required CI 成功再 merge。已有架构/helper 验收按输入是否变化复用。
2. 新 canary 前先完成零模型 preflight：实际 packaged helpers、目标 routing/protection、Oracle flags、Docker image、真实工程师身份、原 stopped source 与 continuation claim、预算推导。原失败暴露的漏洞必须在花模型额度前确认消除。
3. 对最终版本建立正式有限 continuation；原 Issues 177/178 不变。正式 observe-revision → authoring/reconciliation/challenge/adoption 走已有恢复协议，不能手写成功 receipts 或跳过真实 guard。
4. 177 通过实际 planning/acquire/Docker worker/verifier/自动 PR，人工 merge；178 通过既有证伪收尾路径。逐 task 自动 closure/worktree/branch cleanup 完成后冻结 final_main_sha。
5. 新会话执行组末 fresh audit，绑定 actual revision evidence、exact SHA、完整两-slot snapshot；调用 accept_group。accepted/accepted_with_followups 可按既有状态机结项；rejected/unverified 保留失败，不能硬关。
6. BRC14 还需核对既有完整 1→2→3→terminal model-free effect 证据和拒绝分支（本次交接没有重新验证该证据，不应补写为已通过）。BRC15 汇总既有 Canary1、BRC15a 历史与 Owner决定、这一次真实 Canary3 和 off→shadow→active/manual 记录。按现行 Sprint 两行收口，不临时扩门。

CLI 入口使用冻结 source `bun <harness-worktree>/src/cli/index.ts`，`--repo` 用绝对路径。执行 worker 的已有入口是 `scripts/contract-run.ts run --repo <acquired-worktree> --contract ... --campaign-handoff <selector.json> --campaign-provider codex-exec`；closeout 使用既有 `campaign closeout`。这些是入口指针，参数与新身份须按当前契约核实，不照抄旧 token。

## 已知浏览器等待问题（仅发生时使用）

Oracle 的原请求可能已获得 GPT 回复却因 ChatGPT DOM 虚拟化缺少早期 turn 而等待。过去通过原 conversation 的正常 UI 向上滚动恢复旧 turn，原 invocation 随即按正常路径捕获证据。不得换 key 重发、手工拼 receipt 或另起调用冒充原结果。当前没有已知运行中的 Oracle 请求；使用前查真实状态。

## 现场保护

共享根 checkout 不用于接手代码改动。PR374 所有实现位于其独立 worktree，旧 `/Users/ancienttwo/Projects/repo-harness-wt-brc1415-active-acceptance` 保留。不要删 canary `.canary-scratch/`、campaign stores、旧 stopped receipts 或其他 pane worktrees。本文是 ignored handoff，未 commit/push，未向第三方发消息。

## 接手進度 — 2026-09-09 19:10 HKT（Claude 接手輪）

已完成：
- PR #375（tests-only Astra fixture 對齊 + waiver）合入 main=3982ebf5；PR #374 併 main、重綁 digest（notes.md）、Windows flake 重跑後 Required CI 綠，合入 main=38490a8c。fleet worktree 停在 PR head fd8e504e。
- 零模型 preflight 全過（Oracle 0.20.0 pin、Docker image、routing、protection、helper trustedPackage）。舊 OAuth 368611e4 已過期但 principal 解析只讀 mapping，acquire 可用；MCP server 18765 已死、Docker 路徑不需要它。
- Campaign `byok-brc1415-20260909-packaged-delivery`：grant 37225101…（expires 12:23:54Z）；observe-revision verified（294be4e7）；author 更新 #177；slot02 edit_issue 刷新 #178 marker；三次 provider call。

失敗與現狀：
- 我在 adopt 前先發了 `start_group`（依研究代理的錯誤推斷）。adopt 的 `requireIssueBatchAuthority` 要求 `group_preparing`（issue-batch-observer.ts:67），group_running 回不去。campaign 已 canonical stop（`.canary-scratch/packaged-stopped.json`，evidence sha256:49782c57…，provider_calls=3，open=[]）。
- 鏈結封死：delivery intent 548bc0a5 的 `continuation.json` 已獨占綁定到 packaged-delivery（campaign-authoring-resume.ts:56，immutable 寫入拒絕覆蓋）；packaged-delivery 無 adoption/publication，不能作為新 successor 的 resume 來源（validateAdoptedResumeSource:29）。無 CLI 可釋放 continuation 綁定。
- 正確順序（已寫進 `.canary-scratch/packaged-runbook.md`）：prepare_group → observe-revision → author(+followup) → adopt → step/acquire/worker 全在 group_preparing；`start_group` 只在 audit 前。

待用戶決定（三選一）：
1. harness 產品修正：stopped 且無 adoption 的 successor 釋放其 predecessor 的 continuation claim（或 bindAdoptedResume 允許同源重綁到已 stop successor）→ PR/CI/merge 後重 mint 新 grant 續跑。
2. 操作員手術：移除 delivery intent 的 continuation.json 後直接續跑（繞過 guard，不建議）。
3. 非 resume 的全新 authoring（會產生新 Issue，違反「不重抽」約束）。

## 用戶拍板 — 2026-09-09 19:05 HKT：方案 1，獨立 harness bugfix 工作包
- 邊界原文：/tmp/brc-successor/owner-spec.md；設計：/tmp/brc-successor/design.md（deep-reasoner，綁 main 3a30bd89；#376 未動 campaign 檔）。
- 事實修正：adopt 只建 candidate ref 不移 tip；正確順序 adopt → start_group。runbook 已改。
- worktree /Users/ancienttwo/Projects/repo-harness-wt-successor-replacement，branch codex/campaign-successor-replacement，active plan plans/plan-20260909-1909-campaign-successor-replacement.md（Approved，work-package）。deep-worker 實作中：superseded-<intent> 替換記錄、resolveEffectiveContinuation、assertReplaceableStoppedSuccessor、previous_markers、`campaign prepare-resume`、start_group guard；model-free 測試四組。
- 合併後：零模型 preflight → 一次受限 continuation（新 campaign id，supersedes packaged-delivery，來源 delivery）。不擴額。#178 走正式證偽收尾。BRC14/15 仍未通過。

## 工作包 C 狀態 — 2026-09-09 20:35 HKT
- PR #378 codex/campaign-successor-replacement（gatekeeper PASS，findings 已收；首輪 CI 唯一真失敗是 tests/cli/campaign-planning.test.ts 的子命令精確清單，已補 prepare-resume；head 6de585d8，等 Required CI）。
- canary 零模型 preflight 已用分支 CLI 跑過 `campaign prepare-resume`：verdict replacement_eligible，effective_continuation=packaged-delivery，chain consumption delivery 8 calls/9 turns、packaged 3/3；預覽檔 .canary-scratch/replacement-resume-source.preview.json（正式檔合併後用合併版 CLI 重生成到 replacement-resume-source.json）。
- 新 grant 腳本 .canary-scratch/make-replacement-grant.ts / start-replacement.ts：campaign id byok-brc1415-20260909-replacement-delivery，source=successor-replacement worktree（合併 head），PR 378 + CI run 待填（__CI_RUN__）。cap 依「不擴額」設為 packaged 剩餘：provider 37 / turns 45 / runner 43；時間窗須重開 2h（原窗 12:23Z 已過期，這是唯一無法承接的項目）。

## 工作包 C 續 — 2026-09-09 21:05 HKT
- PR #378 已合入 main=16b8670c（squash；head 6de585d8，CI run 34351391235 success）。
- 合併後正式 `campaign prepare-resume` 被資格檢查拒：packaged grant 12:23Z 過期後 run 5a22b4b3… drift=`unsealed_exhaustion`（無 stop receipt）。倉庫沒有能只做 drift repair 的操作員 verb（repair 只在 lockedStatus 內由 mutating verb 觸發）。派 fast-worker 在同一 contract 加 `repairAutomationBudgetDrift` + CLI `automation budget repair --run-id`（零消耗、只封 receipt）+ 測試；這是本工作包唯一一次直接阻斷的範圍外修補。
- 之後順序：repair packaged run → prepare-resume（正式檔）→ make-replacement-grant（PR/CI 綁定改成新 follow-up PR）→ start → observe-revision → author(supersedes) → adopt → start_group → …

## 續跑啟動 — 2026-09-09 21:40 HKT
- PR #380（repair verb，gatekeeper FAIL→修→複核）合入 main=60ef56c4；frozen source = successor-replacement worktree head e6299a70，CI run 34355766739 success。
- `automation budget repair --run 5a22b4b3…` 已封 packaged run 的 wall_clock_seconds receipt（state budget_exhausted、drift none、consumed 3/3/0 不變）。
- 正式 `campaign prepare-resume`：verdict replacement_eligible；replacement-resume-source.json 與預覽逐 byte 相同。
- 新 campaign `byok-brc1415-20260909-replacement-delivery`：grant 15f5fab2…，expires 2026-09-09T15:35:33Z（23:35 HKT），caps provider 37 / turns 45 / runner 43，state authorized。scratch 檔前綴 `replacement-*`。
- 順序：observe-revision → prepare_group → author(supersedes) → adopt → start_group → step/acquire → Docker worker → 人工 merge → closeout → #178 not_planned → audit → accept_group。
- 21:50 HKT：observe-revision verified；author（supersedes）更新 #177 → replacement marker；slot02 followup 更新 #178 marker；intent sha256:903ca327…；adopt 進行中（challenge）。provider calls 至此 3。
- 22:10 HKT：adopt challenge_verified、publication f435a737（target ff+push）；start_group → group_running；planning job task 91eeb878…；plan/contract 20260909-2204-brc177-replacement 提交 664cc07；admission execution_ready；acquire 成功 → worktree byok-brc1415-canary-wt-brc177-replacement / branch codex/brc177-replacement（#374 修正在真實目標生效）。
- 坑：acquire 的 plan-to-todo projection 會把 worktree 內 contract 重渲染成模板 → contract-run incomplete_brief；在 worktree `git checkout -- tasks/contracts/<stem>.contract.md` 恢復後 preflight 通過。Docker worker/verifier 重跑中（selector replacement-selector-01.json）。provider calls 至此 4（observe/author/followup/challenge）。

## HARD STOP — 2026-09-09 22:20 HKT：campaign worker 路徑的 contract 雙權威缺陷
- 現象：兩次 `contract-run run --campaign-handoff … --campaign-provider codex-exec` 都在派 worker 前失敗。第一次 `incomplete_brief`（worktree 內 contract 被 acquire 的 plan-to-todo projection 重渲染成模板，`render_contract_file` 無條件覆寫，assets/templates/helpers/plan-to-todo.sh:424-470）；恢復已提交 contract 後第二次 `campaign worker projected contract changed`（src/effects/automation/campaign-worker.ts:127 比對 :78 在 acquire 後對 worktree 檔案重算的 sha，即模板的 sha）。
- 證據：admission/envelope 的 plan proof `contract_sha256 = sha256:e744eb71…`（我提交的 contract，664cc07），worker handoff 卻綁 projection 後的模板 digest；同一 datum 兩個權威，模板永遠過不了 brief preflight。delivery 輪從未到這一步，所以沒被發現。
- 建議修法（未實施，等指示）：campaign-worker.ts:78 改為消費 `acquired.envelope.plan.contract_sha256`（已 admitted 的 proof），並讓 fleet defaultProject / plan-to-todo 在 contract 已存在且非模板時不覆寫（或 projection 後回寫 proof 版本）。附 model-free 測試：authored contract 經 acquire→projection→contract-run 仍以 proof digest 綁定且 brief preflight 通過。
- 現場：campaign replacement-delivery state group_running；lease 已 acquire（task 91eeb878…，worktree byok-brc1415-canary-wt-brc177-replacement，branch codex/brc177-replacement，未跑 worker、無 publication）；budget run 2681068f… consumed agent_turns 9 / runner 8 / acquisitions 1，provider_calls 8，open []，drift none；grant expires 15:35:33Z（23:35 HKT）。兩個 Docker 容器 `repo-harness-campaign-*` 處於 Created（probe 未啟動）未清。worktree 內 plan/notes/review/todos 為 projection 產生的 M 狀態，contract 已恢復為 664cc07 版本。
- 這是本工作包第二個範圍外阻斷，依規則停下等指示；grant 時窗內若無法修復，需 canonical stop 並再走一次替換（新 successor supersedes replacement-delivery；本輪 8 calls 保留計帳）。
