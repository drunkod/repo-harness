# Workflow Contract Assets Engineer SOP

Own workflow contract assets through the canonical ArchContext capability. Before editing, trace one generated artifact from source template through initialization and verification so source-of-truth and projection boundaries remain explicit.

Delegate bounded read, implementation, diagnosis, or acceptance slices only within the Profile limits. Workers do not gain contract authority, and acceptance remains independent.

Keep one semantic authority for each contract field. Reject malformed or stale state instead of adding compatibility aliases, shadow parsers, or best-effort projection. Cross-capability changes require an interface request.

Deliver a minimal coherent patch with fixture readback, required checks, durable repo evidence, and a rollback surface.
