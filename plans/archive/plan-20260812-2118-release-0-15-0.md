# Plan: repo-harness 0.15.0 release preparation

> **Status**: Archived
> **Created**: 20260812-2118
> **Slug**: release-0-15-0
> **Planning Source**: user-approved-plan
> **Orchestration Kind**: host-plan
> **Source Ref**: (none)
> **Artifact Level**: work-package
> **Promotion Reason**: rollback_boundary
> **Verification Boundary**: version consistency, package/tarball proofs, repository required checks, and one frozen-candidate release gate before merge; exact-main CI after push
> **Rollback Surface**: before publish: revert or abandon release-prep; after publish: forward-fix only and never move v0.15.0
> **Spec**: `docs/spec.md`
> **Research**: See `docs/researches/`
> **Task Contract**: `tasks/contracts/20260812-2118-release-0-15-0.contract.md`
> **Task Review**: `tasks/reviews/20260812-2118-release-0-15-0.review.md`
> **Implementation Notes**: `tasks/notes/20260812-2118-release-0-15-0.notes.md`

## Agentic Routing
- Selected route: planning
- Routing reason: Captured from user-approved-plan planning output.
- Source ref: (none)
- Due diligence:
  - P1 map: See captured planning output below.
  - P2 trace: See captured planning output below.
  - P3 decision rationale: See captured planning output below.

## Workflow Inventory
Complete this inventory before implementation. If any line is unknown, keep the plan in Draft and fill it before projection.

- Active plan: `plans/plan-20260812-2118-release-0-15-0.md`
- Sprint contract: `tasks/contracts/20260812-2118-release-0-15-0.contract.md`
- Sprint review: `tasks/reviews/20260812-2118-release-0-15-0.review.md`
- Implementation notes: `tasks/notes/20260812-2118-release-0-15-0.notes.md`
- Deferred-goal ledger: `tasks/todos.md`
- Current checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope authority: `tasks/contracts/20260812-2118-release-0-15-0.contract.md` `allowed_paths`
- Concurrency rule: `.ai/harness/active-plan` selects the active plan for this worktree when present; `.ai/harness/active-worktree` records the owning worktree. If another worktree already owns active work, open or switch to the matching worktree instead of serializing unrelated plans.
- Execution isolation: approved contract-level work projects through `repo-harness run plan-to-todo --plan plans/plan-20260812-2118-release-0-15-0.md` and may start `repo-harness run contract-worktree start --plan plans/plan-20260812-2118-release-0-15-0.md`.

## Approach
### Strategy
Use the captured planning output below as the execution source of truth.

### Trade-offs
| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Captured plan | Preserves the approved Codex Plan or Waza think decision | Requires the captured text to be concrete enough to execute | Use |

## Detailed Design
### File Changes
| File | Action | Description |
|------|--------|-------------|
| See captured planning output | Follow | Implement only the approved scope named below |

### Code Snippets
See captured planning output.

### Data Flow
See captured planning output.

## Risk Assessment
| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Captured plan lacks enough detail | Medium | Execution may need clarification | Stop before implementation if the captured output contradicts repo rules or lacks concrete file targets |

## Task Contracts
- Contract file: `tasks/contracts/20260812-2118-release-0-15-0.contract.md`
- Review file: `tasks/reviews/20260812-2118-release-0-15-0.review.md`
- Implementation notes file: `tasks/notes/20260812-2118-release-0-15-0.notes.md`
- Template: `.claude/templates/contract.template.md`
- Verification command: `repo-harness run verify-contract --contract tasks/contracts/20260812-2118-release-0-15-0.contract.md --strict`
- Active plan rule: this captured plan is written to `.ai/harness/active-plan` and the owning worktree is written to `.ai/harness/active-worktree` unless --no-active is used. Do not infer active execution from the latest non-archived plan.

## Handoff

- Checks file: `.ai/harness/checks/latest.json`
- Session handoff: `.ai/harness/handoff/current.md`

## Promotion Gate

- **Merge/PR unit**: Captured plan `plans/plan-20260812-2118-release-0-15-0.md` is the proposed mergeable execution unit; revise before execute if this is only a checklist step.
- **Rollback surface**: before publish: revert or abandon release-prep; after publish: forward-fix only and never move v0.15.0
- **Verification boundary**: version consistency, package/tarball proofs, repository required checks, and one frozen-candidate release gate before merge; exact-main CI after push
- **Review/acceptance boundary**: `tasks/reviews/20260812-2118-release-0-15-0.review.md` must record pass against the captured acceptance criteria.
- **High-risk surface**: Risks named in captured planning output; keep the plan Draft if risk ownership is not concrete.
- **Why not checklist row**: rollback_boundary

## Evidence Contract

- **State/progress path**: `plans/plan-20260812-2118-release-0-15-0.md` task breakdown, `tasks/todos.md` deferred-goal ledger, `tasks/contracts/20260812-2118-release-0-15-0.contract.md`, `tasks/reviews/20260812-2118-release-0-15-0.review.md`, and `tasks/notes/20260812-2118-release-0-15-0.notes.md`
- **Verification evidence**: `.ai/harness/checks/latest.json`, `.ai/harness/runs/`, and the commands named in the captured planning output
- **Evaluator rubric**: `tasks/reviews/20260812-2118-release-0-15-0.review.md` must record a passing Waza /check style recommendation
- **Stop condition**: all task breakdown items are complete, sprint verification passes, and the review recommends pass
- **Rollback surface**: before publish: revert or abandon release-prep; after publish: forward-fix only and never move v0.15.0

## Captured Planning Output

# repo-harness 0.15.0 release preparation

## Goal

Prepare `repo-harness@0.15.0` from `v0.14.2..main` as a frozen, verifiable release candidate, merge it to `main`, and push the exact candidate commit. Stop before npm publication, tag creation, and GitHub Release creation.

The minor bump is required because the range contains `feat(hook): add repo-level architecture drift changed-set authority`; the remaining behavior changes and fixes are backward-compatible within that new capability.

## Success Criteria

- Every repo-owned release version authority and public current-release projection says `0.15.0`.
- `docs/CHANGELOG.md` contains an accurate `0.15.0` entry for `v0.14.2..candidate`.
- A dated release filing records scope, candidate evidence, pending public actions, and rollback.
- Version consistency, targeted release/version tests, repository required checks, package contents, and tarball install smoke pass on the frozen candidate.
- The workflow contract, receipt, review, and archive sequence closes cleanly; the candidate is merged and pushed to `main`.
- npm remains unpublished and `v0.15.0` remains absent until a later explicit release authorization.

## Scope

- Bump `package.json`, `assets/skill-version.json`, the five README current-release projections, and `scripts/axr7-consumer-e2e.ts` to `0.15.0`.
- Add `docs/CHANGELOG.md` release notes for the changed-set authority, Stop diff cutover, retired journal architecture bit, and cascade-ack cursor fix.
- Add `deploy/release-checklists/260812-repo-harness-0.15.0.md` with exact source range, evidence, pending-publication state, release sequence, and rollback.
- Update only generated architecture/task projections required by the workflow tools for this release slice.

## Non-scope

- No feature, compatibility shim, dependency upgrade, or refactor beyond release metadata and required deterministic projections.
- Do not change the unrelated fake Oracle version string in `tests/cli/chatgpt-browser.test.ts`.
- Do not rewrite historical release filings, archived workflow artifacts, active unrelated plans/contracts, or the legacy `.claude/.skill-version` repository stamp unless a current consistency check proves it is an authority.
- Do not run `npm publish`, create/push `v0.15.0`, create a GitHub Release, or mutate the selected Bun-global installation.

## Constraints

- One source of truth: `package.json.version` must equal both fields in `assets/skill-version.json`; README and AXR7 values are deterministic release projections verified against it.
- The candidate commit must be frozen before expensive release evidence is produced. Existing evidence may be reused only when subject and freshness checks pass.
- Public publish actions require a later explicit authorization and successful post-merge CI.
- After npm publication, correction is forward-only; the version and tag must never be reused or moved.

## P1: Architecture Map

- Release version authority: `package.json.version` plus the enforced workflow stamp in `assets/skill-version.json`.
- Human projections: five READMEs and `docs/CHANGELOG.md`.
- Consumer/package proof: `scripts/axr7-consumer-e2e.ts`, `scripts/check-skill-version.ts`, npm `prepack`, `scripts/check-npm-release.sh`, and `scripts/check-tarball-install-smoke.sh`.
- Operations surface: `deploy/release-checklists/` owns the durable filing; npm registry, git tag, GitHub Release, and Bun-global install are later external mutation boundaries.
- Workflow authority: plan, contract, review, receipt, task status, and architecture projections generated by repo-harness.

## P2: Concrete Trace

`package.json.version` enters npm packing and the bundled hook build; `assets/skill-version.json` supplies downstream workflow/template stamps. The consistency check rejects divergence. `npm pack` runs `prepack`, builds `dist/hook-entry.js`, and produces the exact tarball consumed by the clean-install smoke and AXR7 fixture. After a later publish, registry, tag, GitHub Release, and exact global install readbacks would prove the public state. This slice ends after the candidate is merged/pushed and post-merge CI is known.

## P3: Decision Rationale

Use a single backward-compatible minor release, `0.15.0`, because the range adds a public hook capability and changes its internal authority without a breaking interface. Keep release preparation separate from irreversible publication so a bad candidate can still be reverted. At 10x dirty-tree size, Stop fan-out remains the first known pressure point; it is already deferred with a measured trigger and does not justify broadening this release slice.

Rejected alternative: `0.14.3` is too small under the repository's release rule because `v0.14.2..main` contains a `feat` commit. Rejected alternative: combining publish/tag/Release into preparation would cross the rollback boundary before the user reviews the candidate.

## Public Interfaces

- npm package version becomes `0.15.0`.
- Generated workflow stamp becomes `repo-harness@0.15.0+template@0.15.0`.
- No CLI flags, config schema, environment variables, or external dependencies are added.

## Task Breakdown

- [x] T1 Create the isolated contract worktree and complete its scope/exit contract before implementation edits.
- [x] T2 Apply the `0.15.0` version projections and add changelog/release filing content.
- [x] T3 Run cheap consistency and targeted release/version checks; inspect the exact package file list and tarball install smoke.
- [x] T4 Freeze the candidate, run the full release gate once, and record acceptance evidence.
- [x] T5 Prepare the accepted branch for the contract-worktree closeout continuation, which will merge/push exact `main`, wait for CI, and report the remaining unpublished actions without executing them.

## Verification

- `repo-harness run check-skill-version`
- targeted tests for version/package/hook bundle behavior selected from current coverage
- `npm pack --dry-run --json` or the repository's equivalent package-content inspection
- `bun run smoke:tarball-install`
- repository required checks from root `AGENTS.md`
- once after candidate freeze: `bun run check:release`
- `python3 /Users/kito/.codex/skills/check/scripts/release_gate.py --root /Users/kito/Projects/repo-harness`
- after push: GitHub Actions CI for the exact `main` SHA

## Failure and Rollback

- Before merge/publish: repair within the release worktree or abandon the branch.
- After merge but before publish: revert the release-prep commit and do not create `v0.15.0`.
- After publication (outside this slice): never move/reuse `v0.15.0`; publish a forward patch fix.

## Fragile Assumption

The version sync surface may have changed since 0.14.2. The release-prep implementation must trust current consistency scripts and package readback over the prior checklist's historical file list; any newly proven authority joins scope, while coincidental `0.14.2` fixtures remain untouched.

## Annotations
<!-- [NOTE]: prefixed inline. Claude processes all and revises. -->

- [NOTE]: The frozen release gate exposed one unrelated timing assertion in
  `tests/unit/closeout-runner-guardrails.test.ts`: it ordered lock acquisition
  against producer-process exit, although the production invariant orders lock
  release against provider-group drain. This directly blocks the release and
  is the one permitted out-of-scope repair; the assertion will observe the
  descendant PID at lock acquisition instead of scheduler ordering.

## Task Breakdown
- [x] T1 Create the isolated contract worktree and complete its scope/exit contract before implementation edits.
- [x] T2 Apply the `0.15.0` version projections and add changelog/release filing content.
- [x] T3 Run cheap consistency and targeted release/version checks; inspect the exact package file list and tarball install smoke.
- [x] T4 Freeze the candidate, run the full release gate once, and record acceptance evidence.
- [x] T5 Prepare the accepted branch for the contract-worktree closeout continuation, which will merge/push exact `main`, wait for CI, and report the remaining unpublished actions without executing them.
