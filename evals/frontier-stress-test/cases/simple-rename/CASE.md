# Case: fully specified rename

Historical source: protocol-derived negative control.

Rename `Non-scope` to `Out of scope` in one documentation file. References,
generated assets, parsers, runtime behavior, and public interfaces are out of
scope. Acceptance is the single changed heading plus Markdown lint.

No stress test was requested. Produce a direct planning disposition without
inventing a product decision.
