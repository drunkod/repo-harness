# Lessons Learned (Self-Improvement Loop)

> Capture correction-derived prevention rules here.
> Promote repeated patterns into durable project rules during spa day.

## Template
- Date:
- Triggered by correction:
- Mistake pattern:
- Prevention rule:
- Where to apply next time:

## Active Lessons

- Date: 2026-09-09
- Triggered by correction: projection receipts naming bc2328db were called stale while this checkout's HEAD/main actually remained at bc2328db; only origin/main had advanced.
- Mistake pattern: treating remote-tracking movement, generation stamps, and the executing checkout's identity as interchangeable evidence of a late writer.
- Prevention rule: align HEAD/main/origin reflogs with receipt timestamps and actual manifest content before attributing a reverse write. A noop receipt is not a filesystem write trace; generation timestamp order is not Git ancestry.
- Where to apply next time: projection incident diagnosis; evidence and limits in `docs/researches/20260909-projection-manifest-writer-investigation.md`.

- Date: 2026-09-09
- Triggered by correction: a workflow-evidence digest failure in the combined CI gate suppressed the functional test run.
- Mistake pattern: making functional failure visibility depend on unrelated governance checks passing first.
- Prevention rule: run governance and functional/package checks in independent hosted jobs; keep `Required / CI` as an `always()` aggregate that accepts only success from every dependency. Preserve the complete local/release gate through `scripts/check-ci.sh` with no arguments.
- Where to apply next time: `.github/workflows/ci.yml`; regression coverage in `tests/check-ci-job-split.test.ts` exercises failing lanes and all aggregate status combinations.

- Date: 2026-09-08
- Triggered by correction: a bounded Stop repeatedly replayed the first legacy architecture drift paths and never reached source files later in the backlog.
- Mistake pattern: acknowledging only the whole range while doing unbounded per-path work inside a fixed deadline.
- Prevention rule: persist the frozen delivery range and acknowledge each complete path cascade before starting the next; advance the Git cursor only after the entire frozen batch completes. Keep later commits observable and retain failed items for retry.
- Where to apply next time: Stop and manual architecture drain; see `docs/architecture/modules/runtime-harness/hook-adapters.md` for the at-least-once delivery contract.

- Date: 2026-09-07
- Triggered by correction: BRC audit reproduced old-revision challenge answers passing with a new SHA, and cross-session discussion separated shadow observation from active execution requirements.
- Mistake pattern: promoting sampled content equality and an echoed revision into proof of exact revision access; treating a parallel capacity limit as a throughput promise; placing a provider feasibility probe behind unrelated active-path work.
- Prevention rule: name only what the evidence proves and retain unmet stronger acceptance requirements. Give shadow probes their actual effect/budget prerequisites, record unknown observations honestly, and keep active safety gates separate. A capacity upper bound does not require overlap; deferring an approved requirement requires an explicit scope amendment, not a successful safe stop.
- Where to apply next time: BRC15a/BRC6a, Connector readback, real-provider canaries, and campaign acceptance. Evidence: `docs/researches/20260902-gpt-pro-connector-readback-probe.md`; execution boundaries: `docs/researches/20260905-repair-campaign-sprint-execution-boundaries.md`.

- Date: 2026-09-05
- Triggered by correction: a historical lite SessionStart message prohibited plan authoring after the current edit scope had already become standard and PlanStatusGuard required a plan.
- Mistake pattern: interpreting snapshot ceremony guidance as a permanent session restriction, then reporting a conflict without resolving the current edit scope.
- Prevention rule: lite means no required ceremony for the observed scope, not a ban on user-requested planning. Resolve the proposed edit plus current diff before acting; current risk requirements supersede historical snapshot guidance. Missing-plan diagnostics must name the current profile, reasons and revision and direct standard to one approved plan without disabling the guard.
- Where to apply next time: Effective State projections, SessionStart guidance, edit-time guards, and task continuation.

- Date: 2026-09-05
- Triggered by correction: small tasks and repeated acceptance attempts were running expensive full suites despite existing exact-context criterion reuse.
- Mistake pattern: broad path-based full-suite rules, worker self-execution followed by canonical execution, and reuse declarations left as template comments made the same coverage run repeatedly.
- Prevention rule: justify full-suite necessity by an explicit acceptance/release requirement or an observed gap in focused coverage. Freeze code and declare deterministic reuse eligibility before the canonical acceptance run; worker, reviewer, and closeout consume its subject-bound evidence. After a full pass, bounded edits need focused delta checks and an explicit parent revision of final criteria, not an automatic full rerun on every new subject hash. Preserve the old run as baseline evidence without claiming it passed on the new subject. Keep stateful checks non-reusable. A generic review helper must execute a selected command, not infer a full suite from a manifest.
- Where to apply next time: contract authoring, delegated worker/verifier prompts, Waza verification, and final acceptance retries.

- Date: 2026-08-31
- Triggered by correction: implementation of public Issues #231-#240 against their declared baseline `5da1963` found that #236's native relay/JSON-envelope symbols and #235's `operations.ts` patch path did not exist at that commit; title/body uniqueness and issue completeness had been verified, but the cited executable authority had not.
- Mistake pattern: treating a well-structured issue body plus a matching commit label as sufficient evidence that its paths, symbols, and runtime boundary exist, then dispatching implementation before reopening those authorities at the exact baseline.
- Prevention rule: before publishing or dispatching a code issue, resolve every cited path and symbol at the declared commit and trace one caller-to-side-effect route. If the authority moved, bind the issue to the active owner explicitly; if the runtime path never existed, reject the issue as evidence-invalid instead of inventing the missing subsystem. Count/title/state checks validate issue hygiene, not technical truth.
- Where to apply next time: GitHub Connector audits, generated issue batches, security/concurrency findings, and any handoff whose acceptance criteria depend on named internal symbols.

- Date: 2026-08-30
- Triggered by correction: the first C9 real-provider probe crossed the frozen `codex exec --json` argv and exposed two facts the shim-only C4 tests could not: the contribution adapter searched persisted stdout for raw marker lines even though the provider emits JSONL, and the generic 64 KiB capture ceiling could truncate the JSONL before its final agent message and `turn.completed` usage event. The fake Codex had been emitting the impossible raw shape, so every deterministic collector test passed.
- Mistake pattern: freezing a producer argv and a consumer parser independently while the test double reproduces the consumer's assumption instead of the producer's actual wire; then sizing evidence capture for prose rather than for the full structured event stream whose terminal receipt is required.
- Prevention rule: every provider integration test double emits the exact frozen transport envelope, including terminal identity/usage events, and carries a negative test for the retired shape. Capture budgets are sized against the whole structured stream and must preserve its terminal receipt; if the receipt is missing or the stream is truncated, fail closed rather than parsing a partial prefix or synthesizing output.
- Where to apply next time: Codex/Claude JSONL adapters, process-output evidence stores, provider review runners, delegation collectors, and any test shim standing in for a structured external CLI.

- Date: 2026-08-17
- Triggered by correction: a `minimal_change` enforce/v2 proposal was drafted on the assumption that advice mode had been observing this repo for two months, but the signal loop had never run once — collection needs both gates (`src/cli/hook/mutation-observed.ts:113`, `policy.mode !== 'off' && policy.post_edit_observer`), this repo's `post_edit_observer` was `false`, `.ai/harness/checks/minimal-change.latest.json` did not exist, and `verdict: 'review'` had occurred zero times.
- Mistake pattern: inferring that a per-repo observation mechanism is live from one enabling field (`mode: advice`) while its separate power switch stayed off, then reasoning about thresholds and escalation over a dataset that was never collected.
- Prevention rule: verify a mechanism's default value and its actual energized state separately — read the per-repo switch and confirm the report artifact exists. Before proposing enforce/escalation over any observer, produce its running evidence (report file present, verdict distribution); with zero data, energize collection first and defer the upgrade.
- Where to apply next time: `minimal_change` and other policy-gated observers, hook-chain features with a mode plus a per-phase toggle, and any proposal that argues from "the advisory version is being ignored".

- Date: 2026-08-11
- Triggered by correction: final ship review proved that a protected closeout helper still executed `node --version` from the caller-provided `PATH` before replacing that `PATH` with its minimal environment.
- Mistake pattern: treating canonical path plus a compatible version string as executable provenance; validation executed the untrusted candidate and then promoted it into the protected runtime authority.
- Prevention rule: protected subprocess boundaries must discover runtimes only from explicit product-owned system/install roots, never from caller environment lookup. A negative regression must place a side-effecting fake executable on caller `PATH` and prove it is not executed even during discovery.
- Where to apply next time: helper runtime construction, package-manager/runtime discovery, hook adapters, and any path that promotes an executable into a sanitized child environment.

- Date: 2026-07-26
- Triggered by correction: PR #132 review found that a repo-external target was reported correctly but also counted as an anonymous capability, while NUL-bearing paths and Win32 absolute hook paths diverged from the intended fail-closed and repo-scope boundaries.
- Mistake pattern: using one partition predicate as both reporting classification and validation authority, then treating every non-POSIX-absolute path as repo-relative.
- Prevention rule: partition valid repo-external paths out of capability matching without adding them to `capabilityCount`; leave malformed paths for canonical validation; classify hook paths with both POSIX and Win32 grammars before any repo-scoped filesystem probe.
- Where to apply next time: Effective State target-risk resolution, capability registry adapters, mutation hooks, and cross-platform path tests.

- Date: 2026-07-23
- Triggered by correction: a scope-runaway incident (a branch-cleanup task self-expanded into fixing every CI fault on `main`) exposed that the live `~/.codex/AGENTS.md` had gained a whole `## Sufficiency and Stop Boundaries` section through direct edits while `assets/reference-configs/global-working-rules.md` — the source `repo-harness init` actually distributes — never received it, so freshly initialized machines shipped without any stop-boundary discipline.
- Mistake pattern: patching the installed copy of a distributed artifact and never back-porting to the template that init ships, leaving the authoring surface and the distribution surface with divergent authority.
- Prevention rule: behavioral-rule changes to global working rules land in `assets/reference-configs/global-working-rules.md` first (with the byte-identical `docs/` mirror and the distribution test), then get re-applied to the live installed copy; treat `~/.codex/AGENTS.md` as an installation target, not an authoring surface.
- Where to apply next time: any edit to global working rules content, reference-config templates under `assets/reference-configs/`, and other init-distributed assets that have live installed copies (hooks, skills, agent definitions).

- Date: 2026-07-23
- Triggered by correction: the `verify-sprint` metadata test launched three runs inside one second, then selected the "latest" snapshot using only second-resolution `generated_at`; GitHub CI intermittently read an earlier run and failed even though the behavior under test was correct.
- Mistake pattern: generating multiple uniquely named evidence artifacts but asserting through a timestamp scan instead of binding each assertion to the producer's explicit run ID.
- Prevention rule: tests that create more than one run/evidence artifact must assign deterministic run IDs and read the exact named artifact; do not use timestamp ordering as evidence authority.
- Where to apply next time: workflow helper fixtures, run-trace tests, evidence materializer tests, and any test that emits multiple artifacts into one directory.

- Date: 2026-07-23
- Triggered by correction: EPC-07 added a private `planSlugFromPath` after Effective State had already declared `src/core/state/artifact-parsers.ts` as that symbol's canonical owner, so the merged `main` failed `check-state-boundaries` even though the EPC slice's focused tests passed.
- Mistake pattern: treating a small parser as a harmless local helper without checking the canonical-symbol registry and the latest target branch before final verification.
- Prevention rule: before adding or retaining a named parser/resolver helper under `src/`, check `scripts/check-state-boundaries.ts` and import the registered owner when one exists; after rebasing or merging the latest target, run `bun scripts/check-state-boundaries.ts` before push so concurrent authority changes cannot land a duplicate implementation.
- Where to apply next time: new `src/` materializers, hook handlers, state/evidence projections, and any branch that ports private helpers from an older implementation.

- Date: 2026-07-20
- Triggered by correction: HRD-03's script retirement needed four separate contract amendment rounds because live consumers (installer evidence lists, sync-tool checks, generated-rule emitters, five locale READMEs, nine test files) were discovered one gate round at a time; HRD-04/05 then landed the same class of cutover with a single amendment round each by enumerating first.
- Mistake pattern: starting a retirement/cutover implementation before enumerating every live consumer of the retired surface, so the allowlist grows reactively through repeated fail-fix-regate rounds.
- Prevention rule: every retirement or cutover contract carries a mandatory pre-enumeration gate — before any code, grep the retired filenames AND the surfaces they write across src/, scripts/, tests/, docs/, README* (all locales), assets/, .ai/hooks/; classify every hit live vs historical with evidence; hand back the enumeration for exactly one allowlist amendment round.
- Where to apply next time: any contract that deletes a script/module, renames a field, or moves an authority; the dispatch packet and the contract Scope must both name the gate.

- Date: 2026-07-20
- Triggered by correction: the P0 package's dispatch-scoped test groups were all green locally, but full-suite CI failed on `tests/state/loop-semantics-characterization.test.ts` — a file no scoped group had ever run.
- Mistake pattern: treating a curated test subset as ship evidence; scoped groups leave CI blind spots exactly where cross-surface interactions live.
- Prevention rule: use root `AGENTS.md#Required Checks` and the active contract's exit criteria as the verification authority. Require the full suite only for an explicit contract/release gate or an observed cross-module risk that named focused checks cannot cover; code or test paths alone do not establish that need. Documentation and ledger-only changes use their relevant checks and repository-integrity checks, recording the changed paths and why narrower verification is sufficient.
- Where to apply next time: delegated implementation dispatches and final verification; reference the owning verification policy instead of copying an unconditional full-suite instruction into every packet.

- Date: 2026-07-20
- Triggered by correction: a gatekeeper advisory claimed the Sprint Contract header fallback was dead code and should be dropped; the worker's counter-evidence (base `derive_contract_path()` carries the identical fallback; non-archived Approved plans exercise it) proved the premise false, and the round-2 gate overruled the advisory.
- Mistake pattern: treating a reviewer's dead-code/cleanup claim as fact without verifying the base behavior and searching for real exercisers first.
- Prevention rule: before dropping "dead" code during a parity port, prove deadness against the base SHA (the function that owns the behavior, not a neighbor) and grep for real artifacts that would exercise the path; an advisory that fails this check gets refused with the evidence, and the adjudication is recorded in the contract, not just the notes.
- Where to apply next time: parity-locked ports and any cleanup suggestion arriving from review, including gatekeeper advisories.

- Date: 2026-07-20
- Triggered by correction: HRD-04's worker validated seven parity scenarios and still missed a 1-byte break that only appeared when two sections combined (resume + active sprint); the gatekeeper's self-built combined-scenario differential caught it. Separately, HRD-05's crash-replay fixture asserted a unit function while the production SessionStart path silently dropped the same section.
- Mistake pattern: parity/behavior fixtures that exercise components in isolation while the composition path (section joining, budget filtering, production entrypoint) is where the regression lives.
- Prevention rule: characterization and parity suites must include at least one end-to-end composition case per surface (through the production entrypoint, e.g. `runHook()`), and differential harnesses must combine features, not only test them singly; goldens regenerate at most once per package under a per-field authorized delta policy where decision-semantic fields never move.
- Where to apply next time: every handler cutover's fixture design and every golden regeneration authorization.

- Date: 2026-07-12
- Triggered by correction: the TypeScript 7 upgrade verification exposed truncated JSON from Bun CLI helpers, which cascaded into architecture-queue parse failures and `unknown` review fingerprints.
- Mistake pattern: writing buffered output with `process.stdout.write` or `process.stderr.write` and immediately calling `process.exit`, allowing Bun to terminate before the pipe was fully flushed.
- Prevention rule: CLI paths that explicitly terminate must synchronously write authoritative output with `fs.writeSync` before `process.exit`; keep regression coverage at the consuming JSON and hook-protocol boundaries.
- Where to apply next time: Bun CLI entrypoints and helper commands that return machine-readable output through pipes.

- Date: 2026-07-11
- Triggered by correction: external ship review showed that `install-agent-fleet.sh` granted `workspace-write` from the parsed frontmatter `name`, so a mismatched `gatekeeper.md` declaring `name: fast-worker` crossed the intended privilege boundary.
- Mistake pattern: deriving permissions from self-declared source metadata without first binding that metadata to the trusted managed identity selected by the installer loop.
- Prevention rule: validate filename/registry identity against semantically normalized, structure-aware metadata before any write. Require the declared flat YAML root schema rather than inferring from nested fields, and reject duplicate root keys because an ambiguous parse is never deletion authority; use the authoritative runtime parser for TOML and compare parsed identity literally to the known role set. Apply that exact-known-role restriction to every format so YAML non-string scalars remain ambiguous rather than deletion authority. Text scans and cross-format reinterpretation are not deletion authority. Enforce the parser runtime's real minimum version in both platform bootstrap scripts and the shared CLI boundary reached by every public mutating install/update path; package-engine metadata may be ignored. When multiple runtime locations are supported, test every trusted candidate before failing so a stale PATH entry cannot mask a compatible explicit fallback. Bind later subprocesses to the exact executable that passed validation, and self-upgrade only a runtime owned by its own installer; package-manager-owned runtimes must fail closed with the manager's upgrade command. Missing or unparseable source identity is fatal; derive sandbox privileges only from validated trusted identity. Inspect installed identities before ordinary drift handling so corrected sources repair stale privilege-bearing targets, and base success on the resulting fleet state rather than only on whether remediation happened in the current process. Unresolved recovery must stay non-zero across retries while ambiguous or identity-correct user drift remains untouched.
- Where to apply next time: installers, manifest projections, role registries, and any generator that maps user- or upstream-authored metadata into runtime permissions.

- Date: 2026-07-06
- Triggered by correction: `context-contract-sync sync-latest` repopulated `Pending architecture request` from the latest event's stale `request_file` after the request card had already been archived.
- Mistake pattern: treating architecture event history as the live pending-request authority. Archived cards retain their original `request_file` for audit, so rendering that value directly makes resolved drift look active again.
- Prevention rule: local architecture contracts may display a pending request only when the referenced card currently exists outside `docs/architecture/requests/archive/` and its metadata `Status` is `Pending`; otherwise render `(none)`.
- Where to apply next time: every renderer or hook that projects architecture queue state from `.ai/harness/architecture/events.jsonl` into `AGENTS.md`, `CLAUDE.md`, session-start context, or architecture indexes.

- Date: 2026-07-06
- Triggered by correction: rubric-v2 scope gate + Codex acceptance both flagged `tasks/todos.md` outside hand-filled `allowed_paths`.
- Mistake pattern: stripping tooling-touched default paths (`tasks/todos.md`) from the contract template's `allowed_paths` when hand-filling briefs.
- Prevention rule: keep the template's default `allowed_paths` entries that projection tooling writes to (`tasks/todos.md`); minimal-change applies to scope semantics, not to deleting tooling defaults.
- Where to apply next time: every hand-filled contract brief.

- Date: 2026-07-06
- Triggered by correction: authority-closure T2 — adding one apostrophe ("task's") to `PI_TEMPLATE_CONTRACT` broke `scripts/lib/project-init-lib.sh` at `bash -n` (exit 2, "unexpected EOF while looking for matching") on stock macOS bash 3.2.57.
- Mistake pattern: a `$(cat <<'EOF' ... EOF)`-wrapped heredoc with a QUOTED delimiter still gets its body scanned for quote parity by bash 3.2's `$()` boundary matcher; an odd count of single quotes inside the literal body breaks parsing even though the heredoc content is never interpreted. Minimal 5-line repro confirmed; other `PI_TEMPLATE_*` heredocs had even quote counts by luck.
- Prevention rule: never embed prose with apostrophes inside `$(cat <<'EOF')` on scripts that must run under /bin/bash 3.2; use temp-file indirection (`mktemp` + `cat > file <<'EOF'` + read back) as `project-init-lib.sh` now does. `read -r -d ''` is not a substitute here — it exits 1 at heredoc EOF and every consumer of this lib runs `set -euo pipefail`. Always `bash -n` every template-bearing .sh after editing embedded template text.
- Where to apply next time: any slice that inserts new template/field prose into `scripts/lib/project-init-lib.sh`, `scripts/ensure-task-workflow.sh`, or `scripts/plan-to-todo.sh` heredocs; check quoting mode per heredoc before writing text containing `'`, backticks, or `$`.

- Date: 2026-07-03
- Triggered by correction: a full `bun test` run leaked a real `plans/sprints/20260703-1424-auth-overhaul.sprint.md` + `.ai/harness/sprint/active-sprint` marker into this repo; content matched `tests/sprint-backlog.test.ts:108`'s fixture exactly.
- Mistake pattern: shell-spawning test helpers that pass `env: undefined` (or `env ? {...} : undefined`) to `spawnSync` silently inherit the parent bun-test process's full `process.env`. Since `bunfig.toml` sets `maxConcurrency = 4`, all test files share one process; a leaked/poisoned env var (e.g. `REPO_HARNESS_TARGET_REPO_ROOT`, which `scripts/sprint-backlog.sh`'s WIP cwd-resolution branch trusts blindly) from anywhere in that shared process can silently redirect a "sandboxed" tmp-workspace test into the real repo. Root cause NOT confirmed — isolated single-file and 7-file/172-test concurrent reruns both failed to reproduce; likely needs full 93-file suite load or was a one-off timing fluke. Applied `tests/sprint-backlog.test.ts`'s explicit `SANDBOX_ENV_BLOCKLIST` strip as defense-in-depth regardless.
- Prevention rule: any test helper that spawns a subprocess into a tmp sandbox must construct an explicit `env` object and delete known ambient-leak vars (`REPO_HARNESS_TARGET_REPO_ROOT` and friends) rather than passing `env: undefined`/raw `process.env` through. Product helpers that accept target-root env must also fail closed unless the canonical env root matches the helper's current cwd; do not rely on tests to scrub inherited env. Never trust "isolated run didn't leak" as proof of absence in a shared-process, concurrent test runner.
- Where to apply next time: audit other test files under `tests/` that spawn `scripts/*.sh` copies into `mkdtempSync` workspaces (grep `spawnSync.*cwd` alongside `env: undefined` or omitted `env`) for the same blind-inheritance pattern; revisit once `scripts/sprint-backlog.sh`'s WIP `REPO_HARNESS_TARGET_REPO_ROOT` change lands and re-attempt a full-suite repro with the fix reverted to confirm/deny the exact trigger.

- Date: 2026-07-06
- Triggered by correction: self-caught while writing a new `tests/cli/global-runtime-init.test.ts` spawnSync test for the `install`/`init` optional-dep interactive-prompt change; the new test hung/produced empty stdout+stderr with exit 0 and no thrown error.
- Mistake pattern: calling `spawnSync('bun', [CLI, ...], { env: { ...process.env, PATH: \`${fakeBin}:${process.env.PATH}\` } })` when the test also drops a fake `bun`/`bunx` shim into `fakeBin` for intercepting the CLI's *internal* subprocess calls. A literal `'bun'` command string plus a manual bash-level PATH edit shadows the real bun runtime too — but empirically `child_process.spawnSync` does NOT use `options.env.PATH` to resolve the command executable itself (only `process.env.PATH` of the actual host process), so this specific failure mode does not reproduce via `spawnSync` — the actual bug was elsewhere (docs. below) and cost real debugging time via a misleading manual-bash repro that used shell-level PATH lookup instead of spawnSync's own resolution. [Corrected 2026-07-09, isolated probes under both runtimes: the "does NOT use options.env.PATH" claim only holds under Bun when NO `env` option is passed at all — Bun then resolves via the PATH the OS process started with, ignoring live `process.env.PATH` JS mutations. When `options.env` IS passed, Bun's `spawnSync` DOES resolve the executable via `options.env.PATH` (an injected fake-bin PATH intercepts the command even though live PATH never changed). Node diverges in the omitted-`env` case: it picks up live `process.env.PATH` mutations. So state the branch explicitly before reasoning about spawn resolution: (runtime: bun|node) × (env passed|omitted).]
- Prevention rule: when debugging a `spawnSync`-based CLI test, reproduce with a small throwaway script that calls `spawnSync` directly (inspecting `res.status`/`res.error`/`res.stdout`/`res.stderr`) instead of hand-rolling the equivalent as a bash one-liner with shell PATH overrides — the two are not equivalent and the bash version can send you chasing a phantom shadowing bug. Separately: this repo's existing convention (see `tests/cli/global-runtime-init.test.ts`'s `CLI update refreshes user-level runtime...` test) is to spawn with `process.execPath`, not literal `'bun'`, whenever the test's `env.PATH` is overridden with a fake bin dir; keep using `process.execPath` for consistency even though the literal-`'bun'` shadowing theory itself didn't hold up.
- Where to apply next time: any new `tests/cli/*.test.ts` spawnSync-based CLI smoke test that overrides `PATH` with a fake bin directory; prefer `process.execPath` as the spawn command, and if output is unexpectedly empty, inspect `res.status`/`res.error`/`res.signal` directly before assuming a PATH-resolution shadowing bug.

- Date: 2026-07-09
- Triggered by correction: user report — on a machine with repo-harness 0.8.3 previously installed, `bunx repo-harness install` kept installing 0.8.3 instead of the latest release.
- Mistake pattern: two self-reinforcing layers. (1) `bunx`/`npx` without an explicit `@latest` reuse the already-installed global binary or cache copy and never consult the registry, and README recommended the unpinned form. (2) The running copy's `installCli` step (src/cli/commands/global-runtime.ts) either skips the reinstall when the running copy IS the Bun global package, or installs its own `sourceRoot` copy globally — every path re-installs the version that is already running, so a stale version silently self-perpetuates.
- Prevention rule: any doc or script that bootstraps a CLI via `bunx`/`npx` must pin `@latest` (or an explicit version); an "already installed, skipping" fast path may surface a best-effort registry comparison only behind the explicit `REPO_HARNESS_CHECK_UPDATES=1` opt-in. The lookup must use the running Bun executable, a fixed official registry, and a non-repo cwd so an untrusted checkout cannot supply the package-manager binary or registry config. The upgrade path (`repo-harness update` → `repo-harness@latest`) existed but nothing pointed users at it.
- Where to apply next time: README install snippets, install.sh/install.ps1, and every `installCli`-like self-bootstrap branch that can conclude "already installed" from a potentially stale running copy.

- Date: 2026-07-13
- Triggered by correction: BDD² Phase E2's locked scores revealed three scoring-semantics defects only after reveal — the owner-disagreement aggregation rule was an unfrozen conservative-union algorithm rather than a pre-sealed procedure, evidence scoring counted declared/explicit limitations as unsupported assertions, and the tracked-artifact count came from reviewer judgment instead of being derived from filesystem evidence. Because the defects were only observable post-reveal, the fail-closed rule downgraded every nominal E2 `Kill` to `Reshape` and Phase P stayed unauthorized; correcting them required a whole new sealed E3 rescoring pass rather than a same-run fix (`evals/bdd2/reports/phase-e2-gate.md`, `tasks/notes/20260712-2319-bdd2-phase-e2-shape-adapter-evaluation.notes.md`, `tasks/reviews/20260713-0314-bdd2-phase-e3-scoring-authority.review.md`).
- Mistake pattern: freezing only the treatment/intervention side of an evaluation (Agent outputs, corpus, conditions) before reveal while leaving the scoring authority itself — the adjudication procedure, the definition of an unsupported claim, and which metrics are reviewer-judged versus mechanically derived — informally specified. Defects in that authority are invisible until scores are already revealed, and a revealed run cannot self-correct without a whole new sealed pass.
- Prevention rule: before any reveal, freeze the full scoring authority as explicitly as the interventions: the exact disagreement/adjudication algorithm (not just "owner resolves"), the boundary between an unsupported claim and a disclosed limitation, and which fields are reviewer-judged versus mechanically derived from artifacts (for example filesystem state). Leaving any of those three implicit is equivalent to an unfrozen gate, not a scoring detail.
- Where to apply next time: any evaluation/benchmark work-package that separates treatment execution from scoring — BDD²-style Agent-panel evaluations, benchmark matrices under `evals/`, or any protocol using blinded reviewer packets plus adjudication — freeze and review the scoring/adjudication spec itself pre-reveal with the same rigor as the corpus and conditions.

- Date: 2026-07-14
- Triggered by correction: external GPT review of the Cjbuilds/Codex-Orchestration comparison corrected the same analysis twice — runtime model verification already existed in `assets/hooks/subagent-start-context.sh` (the "we lack runtime identity accounting" claim was wrong), and its credit-weighting critique nearly got applied to `scripts/run-harness-profile-benchmark.ts`, whose raw-token comparison is same-provider/same-model across harness profiles and therefore valid.
- Mistake pattern: treating "cost savings" as one metric. A raw-token delta is only a valid savings claim when both arms run the same model on the same provider; a cross-model routing claim ("delegating to the cheaper model saved X") stated in raw tokens misprices the delta because per-token cost differs by model, and attributing usage to a model that was never runtime-confirmed misattributes it entirely.
- Prevention rule: cross-model cost attribution must satisfy two conditions before it enters any report or KPI: (1) the execution's model identity is runtime-confirmed by SubagentStart role-routing evidence (`status: verified`), not by config files or child prose; (2) the comparison is stated in model-weighted credits or currency, never raw tokens. Same-model harness-profile comparisons may stay raw-token. Executions without confirmed identity are reported as unattributed, not credited to the cheaper model's ledger.
- Where to apply next time: any future fleet-savings claim, `evals/` cost reports, extensions of `scripts/run-harness-profile-benchmark.ts` that mix models or providers, and any KPI derived from delegation usage.

- Date: 2026-08-09
- Triggered by correction: the ArchContext projection runtime correctly emitted Mermaid-only Markdown with zero HTML artifacts, but the sibling `repo-harness-architecture` skill, architecture queue prompt, generated contract blocks, and reference templates still advertised or discovered optional standalone HTML.
- Mistake pattern: proving one producer path while leaving a second agent-facing instruction path with contradictory output authority; a zero-artifact E2E does not prove that every dispatched agent receives the same contract.
- Prevention rule: architecture diagrams have one product artifact format: evidence-backed Mermaid fenced blocks inside architecture Markdown. The external `mermaid` skill may assist authoring and review, and a pinned CLI may validate renderability, but neither owns an HTML product artifact. Gates must scan generated skills, queue prompts, templates, contract projections, and consumer fixtures together.
- Where to apply next time: architecture projection releases, repo-harness skill/template changes, architecture queue instruction changes, and every adoption or runtime E2E that claims Mermaid-only output.

- Date: 2026-07-18
- Triggered by correction: the session-stop handoff generator's auto-derived "Exact Next Step" ordered `contract-worktree cleanup --slug closeout-authority-bootstrap` on a worktree holding 34 modified files of live unmerged WIP plus 4 untracked workflow artifacts; the instruction survived into a fresh session's takeover packet and was nearly executed. Separately, the same Stop hook regenerates `.ai/harness/handoff/current.md`/`resume.md` on every session/subagent stop, so a hand-authored dispatch packet written into those files was silently clobbered back to the wrong instruction within minutes.
- Mistake pattern: the next-action derivation calls a contract worktree "merged" from a pure branch-ancestry check (`branch tip is ancestor of main`), which is trivially true for any branch with zero unique commits — including a worktree whose entire value is uncommitted work. Consumers of the packet then treat "merged, clean up" as verified state. Compounding it, the single-slot handoff files are hook-owned mutable state, not durable authored documents, so anything hand-written there has hook-regeneration lifetime.
- Prevention rule: before any worktree cleanup/removal, verify emptiness of the working tree itself (`git status --short -uall` in that worktree), never branch ancestry alone; a dirty or untracked-carrying worktree is live work regardless of branch topology. Hand-authored dispatch/handoff packets must live in their own files (e.g. `.ai/harness/handoff/<topic>-dispatch.md`) with `current.md`/`resume.md` at most pointing at them; treat the canonical pair as regenerated projection, not storage.
- Where to apply next time: `workflow_write_handoff`'s next-action derivation and `contract-worktree cleanup`'s merged-check (both should fail closed on dirty worktrees — candidate fix belongs to the closeout-authority lane); any future parallel-session dispatch packet; any takeover session inheriting an "Exact Next Step" that names a destructive command — re-verify the target's state before executing it.

- Date: 2026-08-11
- Triggered by correction: issue #174 showed ChatGPT Connector reconnects could create replacement Streamable HTTP sessions without closing the superseded session, so the fixed-size store reached 64 records and rejected every later `initialize` with `SESSION_LIMIT_REACHED` until process restart or the 30-minute TTL elapsed.
- Mistake pattern: treating a hard session cap plus eventual TTL cleanup as sufficient lifecycle control when the remote client may abandon sessions without sending `DELETE` and without producing a transport `onclose` event.
- Prevention rule: bounded remote-session stores must reserve capacity atomically, track in-flight requests, and reclaim the least-recently-used idle record at the admission boundary; active requests remain fail-closed and lifecycle outcomes must be observable as created, closed, expired, and evicted counters.
- Where to apply next time: MCP Streamable HTTP transports, reconnecting connector protocols, authorization runtime stores, and any bounded in-memory lifecycle store whose clients can disappear without an explicit close handshake.

- Date: 2026-08-18
- Triggered by correction: after rebasing a contract worktree onto a moved `main`, `verify-sprint --prepare-acceptance` reported `Sprint verification failed` even though the contract itself passed (`total=15 failed=0 status=Fulfilled`). The real cause was only visible in the run snapshot: `.ai/harness/worktrees/<slug>.json`'s `base_commit` still pointed at the pre-rebase base, so `git_changed_files_list` diffed against a stale ref and attributed main's own commits (`docs/architecture/*`) to the slice, tripping `allowed_paths_check`. Hand-editing `base_commit` to the new base cleared it immediately.
- Mistake pattern: treating the worktree metadata's `base_commit` as derived state when it is stored state. Rebase moves the branch but nothing updates that file, so every scope gate afterwards measures the diff from a base that no longer exists on the branch. The failure surfaces as a scope violation naming files the slice never touched, and the top-level message (`Sprint verification failed`) points at neither the base nor the rebase — the diagnosis cost is entirely in the gap between the symptom and the cause.
- Prevention rule: after any rebase of a contract worktree, refresh `.ai/harness/worktrees/<slug>.json`'s `base_commit` to the new merge base before running any gate. When a scope gate fails naming files outside the slice, check `base_commit` against `git merge-base HEAD origin/main` before investigating the files themselves. Note this metadata is gitignored runtime state: it dies with the worktree, so a rebuilt worktree for the same contract will reproduce the same false positive.
- Where to apply next time: any `repo-harness run contract-worktree` slice that rebases before closeout, `verify-sprint`/`verify-contract` scope-gate failures listing unrelated paths, and any future work making `base_commit` self-refreshing or making the gate's failure message name the stale base.

- Date: 2026-08-18
- Triggered by correction: `contract-worktree finish --merge` on `codex/typed-lock-transient-errors` (base `1d1b702a`) published its synthesized commit onto a `main` that had advanced by two commits (`35346e39` stop-handler enforce-gate fix, `e18937bd` lite-enforce-gap closeout). The publication tree was byte-identical to the lifecycle HEAD, so it silently reverted both commits' content — deleted `tests/stop-handler.test.ts` additions, restored the pre-fix `stop-handler.ts`, and removed the lite-enforce-gap plan/contract/notes/review — despite zero file overlap with the package. History was intact (the commits stayed in the parent chain), so recovery was `git checkout e18937bd -- <clobbered paths>` plus a restoration commit (`61b60e98`).
- Mistake pattern: treating "no file overlap with the advanced commits" as merge safety when finish's publication is tree replacement, not a content merge. The documented invariant "the target base must remain frozen through publication" was violated by the environment (a parallel session landing on main), and finish did not fail closed on the moved base — it happily overwrote. The gatekeeper's pre-ship check reasoned about content-level conflicts, which is the wrong model for a tree-replacement publisher.
- Prevention rule: before `contract-worktree finish --merge`, compare the recorded worktree `base_commit` against the current target head; if the target advanced, rebase the worktree onto the new head (then refresh `base_commit` per the sibling 2026-08-18 lesson), re-run the gates, and only then finish. After any finish, diff `target@{1}..target` and verify every changed path maps to the package's goal manifest; unexplained deletions of sibling-session artifacts mean the publication overwrote an advanced base. Candidate product fix: finish should refuse when target head is not the recorded base.
- Where to apply next time: every `contract-worktree finish --merge` in multi-session periods, the worktree-merge-authority lane (`tasks/contracts/20260817-2055-worktree-merge-authority.contract.md`), and any post-merge sanity check in gatekeeper dispatches.

- Date: 2026-08-18
- Triggered by correction: `tasks/todos.md`'s T1 entry — "`isTransientResolutionInstability` is string-matched instead of typed and misses `lost exclusive lock ownership`" — was already fully resolved by `d2950ae7 feat(contract): complete typed-lock-transient-errors` at the moment it was written. The deferred-goal ledger and the work-package that closed the goal were produced by parallel sessions in the same window: the research pass read a pre-`d2950ae7` state, and its findings landed as durable entries after the fix shipped. A `codegraph_explore` before implementing showed `isTransientResolutionInstability` already reduced to two `instanceof` checks, all three lock throws already raising `ExclusiveLockContentionError` (`exclusive-directory-lock.ts:338,379,412`), and `STABILITY_UNSTABLE_MESSAGE` gone from the tree with every named test migrated to `toBeInstanceOf`. The other entries in the same batch were spot-checked and remain accurate (`recordDirectChildProcess` still has zero call sites; `hook-events.jsonl` is 40MB with no rotation), so the staleness was per-entry, not batch-wide.
- Second entry from the same batch, different failure mode: T4 claimed `event-telemetry.ts`'s `child_processes` metric "lies about measurement completeness" because it is declared `ALWAYS_COMPLETE` while `recordDirectChildProcess` has zero call sites and nine hook files fork Git/Bun. The metric's actual contract is narrower than the name suggests — `scripts/hook-dispatch-diet-report.ts:418` states it counts "direct route-runtime children, not internal Git/Bun plumbing", and `tests/hook-runtime.test.ts` ("typed handlers do not consult a route filesystem or spawn a route child") plus `tests/hook-runtime-characterization.test.ts` both pin it to 0. The zero call site is the invariant, not a gap: `recordDirectChildProcess` is the sentinel that goes non-zero if a route regresses to the retired `run-hook.sh` shape (HRD-09). Wiring it into the internal spawn sites would have broken two passing tests to "fix" a metric that was already correct.
- Mistake pattern: two distinct ways a detailed entry can be wrong. (a) Stale — accurate when written, closed before it was read. (b) Misread — never accurate, because a symbol's contract was inferred from its name and surrounding code rather than from the tests and report legend that define it. Citation density is evidence of research quality at write time, not of present validity, and it does not distinguish these two cases: T1 and T4 were equally well-cited and equally wrong. Deferred goals written during a parallel-execution window are the highest-risk class for (a); metrics, counters, and other numeric contracts are the highest-risk class for (b), because a plausible wrong reading of "child processes" produces a convincing bug report.
- Prevention rule: re-verify a deferred-goal entry against current code before implementing it, not after. One `codegraph_explore` on the named symbols is enough and costs far less than the implementation. Before treating a zero-call-site function or an always-zero metric as a gap, grep the test suite and any report legend for the symbol: an assertion pinning it to zero means the zero is the contract and the call site is deliberately absent. When an entry turns out to be closed or wrong, delete it and fix any sibling entry that referenced it by position (T2 here cited "the T1 fix above"); leaving it in the ledger makes the next reader re-derive the same conclusion. Spot-check the rest of the batch rather than assuming the whole batch shares one verdict — T2 and T3 from this same batch were re-verified and remain accurate.
- Where to apply next time: every `tasks/todos.md` entry picked up for execution, especially batches stamped with a research-pass provenance line; also any handoff, workstream, or notes artifact authored by a session that ran concurrently with implementation work on the same capability.

- Date: 2026-08-18
- Triggered by correction: a Stop hook blocked on `[ArchitectureProjection] Strict projection failure gate blocked Stop: archctx projection failed: ... "Worktree digest changed before apply"`. The natural response — re-run the drain, inspect the queue, re-run again — is exactly what burns the job. `job-3abf379dcfff3a06a8810416` went pending -> running -> pending three times while being diagnosed, exhausted `MAX_ATTEMPTS`, and landed in dead-letter. Recovery was `repo-harness architecture-projection retry-dead-letter --job-id <id>` (which resets `attempt` to 0) followed by `drain` **in the same Bash invocation**; it succeeded on the first try, receipts 51 -> 52.
- Mistake pattern: diagnosing a failure whose cause is the diagnosis. archctx captures a worktree digest at plan time and re-checks it before apply, failing closed if the tree moved. Every agent tool call ends by firing hooks that append to `.ai/harness/runs/hook-events.jsonl` and touch `.claude/` session files, so each `ls`/`jq` used to inspect the queue both moved the digest and re-triggered a Stop drain against it. Observer and observed share one worktree; the loop is self-sustaining and reads as a flaky provider rather than as self-interference.
- Prevention rule: on `Worktree digest changed before apply`, do not re-run the drain interactively and do not poll the queue between attempts — each poll costs one of three attempts. Chain the whole recovery into a single shell invocation (`retry-dead-letter --job-id <id>` then `drain`), because no hook fires between commands inside one invocation. Check the outcome once, afterwards. The same applies to any drain run while an agent is actively editing: batch the inspection, or accept that the drain will fail closed.
- Where to apply next time: any Stop blocked by the strict projection failure gate, any `architecture-projection drain` run mid-session, and any future work making the drain's snapshot check ignore gitignored runtime-evidence paths (`.ai/harness/runs/`, `.claude/`) so agent activity stops competing with it.

- Date: 2026-08-18
- Triggered by correction: a parallel session landed `cb6d9b51 chore(helpers): sync check-architecture-sync projection missed by f385d808` — my change to `scripts/check-architecture-sync.sh` never reached its distribution copy under `assets/templates/helpers/`. Diagnosing that miss produced two more wrong conclusions in a row. First, a `cmp` sweep over all 54 helper pairs reported `capability-resolver.ts` as drifted; it is not, it is an intentional standalone projection carrying `// @generated-from src/core/capabilities/registry.ts sha256:97d0cdf...` (the repo copy imports from `src/core/`, which downstream repos do not have — the declared sha still matches). Second, I concluded no consistency check existed and proposed adding one; `scripts/sync-helper-sources.ts` and `tests/unit/helper-projection-drift.test.ts` were already there, and reverting the template to its pre-`f385d808` state makes that test go from `4 pass` to `1 fail` — it would have caught the original miss exactly.
- Mistake pattern: concluding "no mechanism exists" from a search that was too narrow, then proposing to build what is already built. The grep was `scripts/*.sh tests/*.ts` filtered by cmp/diff keywords, which structurally cannot see `tests/unit/` (subdirectory) or `sync-helper-sources.ts` (verb-named, no keyword in the body). The upstream cause of the whole chain is simpler: the miss shipped because I ran targeted test files instead of the `bun test` that heads this repo's Required Checks — the guard was never absent, it was never invoked.
- Prevention rule: run the full `bun test` before any commit that touches `scripts/`, `src/`, or `assets/`, not a curated subset — this repo's helper-projection, state-boundary, and contract guards live in files a targeted run will not select. Before asserting that a check, guard, or sync mechanism is missing, search by artifact shape rather than keyword: `rg --files -g '*sync*' -g '*drift*' -g '*projection*'` across `scripts/` and all of `tests/**`, and read any `@generated-from` header before treating a diff between two copies as drift. A byte difference between an authoring copy and a distribution copy is not automatically a defect; a declared provenance header with a matching digest means the difference is the contract.
- Where to apply next time: any edit under `scripts/` (every file there has an `assets/templates/helpers/` twin), any proposal that starts with "there is no check for X", and any `cmp`-based sweep over generated or projected file pairs.

- Date: 2026-08-18
- Triggered by correction: a Codex session built an Obsidian long-term-memory system and recorded in its own project page that the retrieval/write-back protocol had been "接入 Codex 全局规则" and that a Friday 18:00 maintenance automation was "已启用". Neither was true: `~/.codex/AGENTS.md` was 227 lines with zero matches for `memory|记忆|回写|obsidian`, and `~/.codex/automations/` was an empty directory with no matching scheduler row. The system therefore never triggered on its own; its two write-backs happened only because the authoring session did them by hand, and both degraded into commit-SHA and CI-run-id changelogs.
- Mistake pattern: recording an integration as done in the artifact that describes the integration, rather than in (or verified against) the file that would actually execute it. A wiring claim that lives only in prose is worse than no wiring, because it stops anyone from looking for the missing hook-up. The same session also wrote absolute paths under a home directory (`/Users/kito`) that a later machine rename invalidated, so the recorded entrypoints pointed at nothing.
- Prevention rule: a claim of the form "X is now wired into Y" must be verified in the same turn by grepping Y and pasting the hit, or it is not written down. When a memory or design note records an entrypoint, prefer a repo-relative path or a resolver the tool owns (e.g. read the configured root from config) over an absolute path; absolute paths fail silently when the checkout moves or the account is renamed. When a durable-memory surface produces content, apply the exclusion gate before the value gate: anything git, a package registry, a code-hosting platform, CI, or a re-runnable command already records may only be linked, never restated.
- Where to apply next time: any "already integrated / already enabled" statement in plans, contracts, notes, or vault pages; `assets/skills/obsidian-memory/SKILL.md` persist flow; and any future automation whose only evidence of existence is a sentence claiming it exists.

- Date: 2026-08-19
- Triggered by correction: the 0.15.3 release candidate hit three consecutive acceptance-gate BLOCKED verdicts before the receipt could bind. Two were self-inflicted at candidate-prep time: the auto-captured release contract's `exit_criteria` carried `check-architecture-sync.sh` and `check:release` (the second transitively re-runs the first through `check-ci.sh:66`), both of which can never pass inside the bounded verifier on this machine because `scrubHarnessEnv()` strips `REPO_HARNESS_NODE_BIN` while archctx needs Node >=24 <26 and the protected PATH carries none; the third was an empty `Change Assessment` oracles block, which the strict deploy/release path categories reject with `irreversible_effect` + `oracle_gap`. The 0.15.2 release contract already had the right shape on both counts: no architecture gate in `commands_succeed`, and the two oracle declarations (`release-deterministic-gates` deterministic_test + `published-package-runtime-readback` runtime_readback) present from candidate prep.
- Mistake pattern: preparing a recurring-ritual artifact from the generic template instead of from the last successful instance of the same ritual. The template encodes the generic contract shape; the previous release contract encodes every machine-specific and profile-specific accommodation the ritual has already paid for. Each missing accommodation surfaced one gate round late, serially, at ~15 minutes per round.
- Prevention rule: when capturing a release (or any recurring-ritual) contract, diff the fresh capture against the previous release's archived contract before running the first gate, and justify every divergence explicitly. Separately: `npm publish` with a wholesale-expired token fails as `E404 Not Found - PUT` with no auth prompt, not E401 — check `npm whoami` first and run `npm login --auth-type=web --browser=false` before diagnosing the registry.
- Where to apply next time: the 0.16.0 release (diff its contract against `tasks/archive/` copy of the 0.15.3 one), any `capture-plan`-generated contract for a ritual with an archived predecessor, and any npm publish failure that reads as a missing package.

- Date: 2026-08-19
- Triggered by correction: the WP2 board-projection plan froze its architecture doc at `docs/architecture/modules/workflow-engine/shared-coordination-plane.md` (the spec's suggested path), and the executing worker hit a contract-internal contradiction mid-flight: that directory is a closed, capability-derived set. Any hand-written `.md` there that is not some capability node's `architecture_module` fails the resolver's orphan check (`scripts/capability-resolver.ts`), and the module count plus generated shape are pinned by `tests/architecture-projection-e2e.test.ts` — so the contract's `files_exist` criterion contradicted its own `bun test` criterion. The fix was an orchestrator amendment relocating the doc to the `docs/architecture/` root beside `effective-state-authority.md` and its siblings; minting a capability node was rejected because the coordination plane spans four top-level source areas, not one longest-prefix boundary.
- Mistake pattern: freezing an artifact path into a plan/contract without checking whether the target directory is generated or derived. The two gates that enforce the closed set are non-obvious — neither lives next to the directory it protects, and nothing in the directory itself says "derived".
- Prevention rule: before a plan or contract freezes any new file path, classify the parent directory: grep for a resolver/orphan check naming it and for tests pinning its count or shape (`rg -l 'orphan|architecture_module' scripts/ tests/`). Generated/projected directories admit only projection outputs; human-owned architecture prose in this repo lives at the `docs/architecture/` root. A capability node is only the right legalization when the documented surface actually is one longest-prefix code boundary.
- Where to apply next time: any plan freezing paths under `docs/architecture/modules/`, `.ai/context/`, or another projection-owned surface; WP3's hook-slice docs; and contract reviews — treat a frozen path in Goal + allowed_paths + exit_criteria as a claim to verify, not a given.

- Date: 2026-08-20
- Triggered by correction: shipping `native-subagent-boundary-dedup` took four gate rounds because `main` advanced three times mid-ship (`07a5d63a` -> `c5ab577d` -> `ddf53f8c`) and the `07a5d63a` archive sweep had left `main` itself red: it moved `plans/sprints/20260722-0001-evidence-projection-convergence.sprint.md` into `plans/archive/` while `tests/evidence-residue-scan.test.ts:198` still read the old path (2 ENOENT failures). Two later contracts merged over that red because their verification evidence froze before the sweep landed — the exact stale-base class the gates exist to stop reproduced inside the gate chain itself. Every reconciliation costs a full re-freeze (~13 min `bun test`) because a rebase invalidates the frozen subject and the AcceptanceReceipt.
- Mistake pattern: an archive sweep treats plan/sprint file moves as pure bookkeeping, but tests hold literal paths into `plans/` (same class as the README literal-string coupling); and parallel sessions shipping to one shared local `main` serialize badly — each `main` advance inside a freeze window forces another rebase + re-freeze + re-receipt round.
- Prevention rule: before committing an archive sweep, grep the moved filenames across `tests/` (`rg -l 'plans/sprints/' tests/`) and update literal paths in the same commit. When shipping under active parallel sessions, secure a quiet merge window before starting the receipt -> finish chain, or budget ~15 minutes per `main` advance. A base-inherited red is carried as an explicit paths-only fix commit with a contract In-scope line — never merged over, never worked around by weakening assertions.
- Where to apply next time: any `chore(workflow)` sweep touching `plans/` or `tasks/`; any multi-session day where two contracts race to `main`; any `contract-worktree finish` refusal reading "target branch advanced past this worktree's fork point".

- Date: 2026-08-21
- Triggered by correction: `tests/cli/global-runtime-init.test.ts` "the installed candidate handoff checks its closure before projecting host files" failed on ubuntu CI (`expect(result.status).toBe(1)` got 0) because it sabotaged the Node runtime by putting a fake `v22.14.0` node on `PATH` only. Since `fa2a4b8d`, `resolveCompatibleNodeRuntime` has a tier-3 trusted-candidate scan (`src/effects/runtime/node-candidates.ts:22-47`) that probes absolute machine paths (`/usr/local/bin/node`, `/opt/hostedtoolcache/node/*/*/bin/node`, `~/.local/bin/node`, nvm dirs); CI's real Node 24 satisfied the closure check and the run succeeded. Local macOS passed only by luck (homebrew node 26.5 is over the `<26` bound).
- Prevention rule: a test that sabotages Node runtime resolution must pin the tier-1 explicit authority (`REPO_HARNESS_NODE_BIN`) at the incompatible binary, not just poison `PATH` — tier-3 scans absolute machine paths that no child-env sanitization can hide, so a PATH-only sabotage is host-dependent and green by accident.
- Where to apply next time: any runtime-resolution or toolchain-closure test in `tests/cli/`; any assertion whose failure message comes from a lower resolver tier than the one the test believes it is exercising.

- Date: 2026-08-21
- Triggered by correction: main CI was red on `f565d319` (run 32409039529) at `tests/architecture-projection-restamp-cli.test.ts` "drain output shape", while every local run was green. The fixture wrote an empty policy (projection provider disabled) and left `docs/architecture/.projection-manifest.json` tracked-dirty; `src/cli/commands/architecture-projection.ts:45-50` routes a `disabled` drain through the legacy cascade once per changed path, and `src/cli/hook/mutation-observed.ts:855-858` fails that cascade when no `repo-harness` runner resolves from `PATH`/`REPO_HARNESS_CLI`. Developer machines carry a global `~/.bun/bin/repo-harness`, so the branch silently succeeded locally and only CI saw `legacy architecture cascade runner is unavailable`.
- Mistake pattern: asserting a green local run for a test whose failing branch is gated on a developer-global binary. The assertion under test (drain JSON key order, no publication keys) never needed a dirty tracked path at all — the dirty setup came from a shared `fixture()` written for the publish tests and dragged an unrelated PATH dependency into a pure shape lock.
- Prevention rule: any test that can reach `repoHarnessRunnerAvailable` (the legacy cascade, Stop-time handlers, hook mutation paths) is only meaningful when run PATH-scrubbed or on CI: `PATH="$(echo "$PATH" | tr ':' '\n' | grep -v '\.bun/bin' | paste -sd: -)" bun test <file> --timeout 60000`, with `REPO_HARNESS_CLI` unset. Also keep a shared fixture's incidental dirt out of tests that do not assert on it — parameterize the fixture instead of inheriting a worktree state whose only effect is to enter an unrelated code path.
- Where to apply next time: any new test in `tests/architecture-projection-*`, `tests/cli/hook*`, or anything invoking `architecture-projection drain` against a dirty tracked path; and any "green locally, red on CI" report where the CI error text names an unavailable runner or helper.

- Date: 2026-08-21
- Triggered by correction: upgrading Bun 1.3.14 -> 1.4.0 turned `tests/harness-benchmark-matrix.test.ts` "rejects Git-clean install-profile mode drift" red (`expect(mode & 0o777).toEqual([0o777, 0o777])` got `0o755`). The test proved `assertBenchmarkSubjectUnchanged` rejects install-profile drift, but it manufactured that drift by relying on a bun side effect: `bun add -g <local source dir>` used to chmod the source package's bins to world-writable. Bun 1.4 stopped doing that, so the drift carrier vanished and the rejection chain had nothing to reject. A/B with a sandboxed 1.3.14 binary against identical repo content and `node_modules` isolated the bun version as the only variable.
- Mistake pattern: a test that needs a precondition (here: mode drift) obtains it as a free side effect of a toolchain command instead of injecting it, silently coupling the assertion to a toolchain implementation detail nobody declared. The failure then reads as a product regression at upgrade time and costs a full triage cycle to exonerate the dependency refresh.
- Prevention rule: a test must inject the state it asserts on. When a fixture's precondition comes from a package manager, compiler, or VCS side effect rather than an explicit call, replace it with the explicit call (`chmodSync`, explicit write, explicit mtime) and comment why. Before blaming a dependency bump for a toolchain-upgrade failure, A/B the old toolchain binary in a sandboxed `BUN_INSTALL`/prefix against the same tree to separate toolchain change from dependency change.
- Where to apply next time: any test asserting file modes, timestamps, lockfile bytes, or install-tree layout produced by `bun add`/`bun install`; any future bun, node, or git major upgrade triage where one test goes red and the dependency diff is the convenient suspect.

- Date: 2026-08-21
- Triggered by correction: the first Bun 1.4 landing (`b6dee923`) passed the full macOS suite 2757-green and was pushed to `main`, then had to be reverted (`ead6b216`) because ubuntu CI run 32404506563 failed `tests/skill-hooks.test.ts` "executeHookScript runs a successful script" with an unhandled EPIPE (errno -32, syscall `send`) from the child-stdin write in `scripts/run-skill-hook.ts` — the hook script itself exited 0. Bun 1.3 swallowed that stdin-write EPIPE on Linux; 1.4 propagates it. macOS could not observe the regression at all, so every local gate was structurally blind and the revert round cost a full re-freeze.
- Mistake pattern: treating a toolchain upgrade as verified by the developer's own platform. A runtime bump changes syscall-level and stream-teardown behavior that differs per OS, so a single-platform green is not evidence about the platform CI actually runs. The contract's exit criteria named only local commands, which made the blind spot invisible to the gate rather than merely unlucky.
- Prevention rule: a contract that bumps a runtime, compiler, or package-manager version must carry a Linux (i.e. CI-platform) verification surface in its own `exit_criteria`, satisfied BEFORE the merge — `docker run --rm -v "$PWD":/repo -w /repo oven/bun:<version> bash -c 'bun install --frozen-lockfile && bun test <targets>'` when docker is available, otherwise a draft PR that triggers the Linux jobs. Mount a clean clone rather than the live worktree so the container's `bun install` cannot overwrite the host `node_modules` with foreign-platform binaries.
- Where to apply next time: any bun/node/tsc/git version bump; any contract whose Change Assessment oracles are all local deterministic tests while the failure surface is cross-platform; any "macOS full suite green" claim used as merge evidence for a runtime change.

- Date: 2026-08-21
- Triggered by correction: issue #209 traced a one-off architecture queue CI failure to `acquireQueueLock`: `openSync(lockFile, "wx")` published an empty lock before the owner JSON write. A process descheduled in that interval could cross the two-second malformed-lock threshold, letting a contender delete a live owner's lock; the original owner then failed during release. The concurrent test compounded diagnosis by piping child stderr without reading it.
- Mistake pattern: treating exclusive file creation as atomic lock-record publication even though the ownership payload is written afterward, then testing only the final exit codes while discarding the subprocess diagnostic stream.
- Prevention rule: when contenders classify or reclaim a lock from its owner record, stage the complete record under a sibling temp name and atomically publish it with a no-overwrite primitive such as same-directory hard-link creation; never expose a parseable authority path before its bytes are complete. Concurrent-process tests must capture stderr and attach it to failed status assertions.
- Where to apply next time: file locks whose reclaim policy parses owner metadata, crash-recovery sentinels, and async subprocess tests that currently assert only an exit code.

- Date: 2026-08-24
- Triggered by correction: the first `repo-harness operator serve` acceptance pass treated a loopback-only TCP bind plus absent CORS headers as a sufficient same-origin boundary. A direct probe with `Host: attacker.example` and `Origin: http://attacker.example` still received the browser-safe Fleet snapshot with status 200 because the Node HTTP handler constructed URLs against the configured bind host and never checked the request authority. That leaves a DNS-rebinding path from an attacker-controlled browser origin to local repository and task metadata.
- Mistake pattern: equating socket reachability with HTTP authority. Binding `127.0.0.1` or `::1` blocks remote TCP peers, but it does not prove that a browser request reaching that socket was addressed to the loopback origin; no CORS response is not a replacement for validating `Host`, and a read-only API can still disclose local state.
- Prevention rule: every browser-facing loopback HTTP service must pin the request `Host` to its configured loopback address plus the actual listening port before routing, and reject any supplied `Origin` that is not the exact same origin. The rejection must occur before collectors or other authority reads run, with regression tests covering hostile Host, hostile Origin, and the valid local request.
- Where to apply next time: local dashboards, OAuth callbacks, MCP transports, dev control planes, and any new localhost HTTP endpoint that exposes repository, credential, process, or workflow metadata.

- Date: 2026-08-24
- Triggered by correction: while validating candidate changes to `scripts/verify-sprint.sh`, `repo-harness run verify-sprint` passed the 18-minute root suite but produced no candidate criterion cache. A same-subject retry began the expensive helper criterion again. `src/cli/runtime/helper-runner.ts` showed why: `verify-sprint` and `verify-contract` are protected helpers, so the installed CLI deliberately ignores `REPO_HARNESS_SOURCE_ROOT` and resolves its packaged helper. The retry was interrupted before a second root suite, and candidate behavior was re-proven through direct source-helper fixtures instead of importing or fabricating a pass cache.
- Mistake pattern: assuming that running the source checkout's CLI entrypoint or targeting a source worktree implies helper source selection. Protected helper resolution is a security boundary; CLI location, target repository, and helper authority are separate facts.
- Prevention rule: when changing a protected helper in this self-host repository, use `bash scripts/<helper>.sh` for direct candidate integration and packaged disposable fixtures for retry/provenance assertions. Use `repo-harness run <protected-helper>` only to verify the currently installed package runtime, and inspect the run snapshot for candidate-only fields before paying an expensive second attempt.
- Where to apply next time: every work-package touching `verify-sprint`, `verify-contract`, or another `PROTECTED_HELPERS` entry in `src/cli/runtime/helper-runner.ts`; release/readback checks must label installed-runtime evidence separately from candidate-source evidence.

- Date: 2026-08-28
- Triggered by correction: the ME3 acceptance follow-up ran its ship order with push ahead of the receipt round. Merges `a61f5b9b` and `a7eeb109` reached `origin/main` before the receipt was recorded, so `reviewed_paths` held only one unrelated dirty file (`docs/architecture/.projection-manifest.json`) and all six actually changed files fell outside the receipt subject.
- Mistake pattern: treating the acceptance receipt as a post-merge closeout step. Its subject is the worktree diff relative to the review base, so once the slice is merged into that base the subject is necessarily empty and the receipt certifies nothing about the work it names.
- Prevention rule: run the receipt round (`verify-sprint --prepare-acceptance` -> `acceptance-receipt record` -> `verify-sprint`) to completion before the slice merges into the review base. The review base comes from `.ai/harness/policy.json#worktree_strategy.review_base` (`origin/main` in this repo) and is resolved at `src/effects/review/diff-fingerprint.ts:67-85`. When dispatching a ship order, sequence the receipt round ahead of merge and push, and treat a recorded receipt as sealing the worktree: any later edit to tracked files (including workflow docs such as review cards) recomputes the subject and makes the receipt stale, so finish all workflow-file writes before recording.
- Where to apply next time: every ship execution order that contains a merge or a push, and any `contract-worktree finish` flow.

- Date: 2026-08-30
- Triggered by correction: the C2 gatekeeper round found a missing sprint-named field in `src/core/collaboration/context-packet.ts` that an earlier grep-based audit had reported as absent from the whole module. The audit was empty for the wrong reason: `src/core/collaboration/common.ts` and `thread-projection.ts` embed literal NUL bytes as domain separators in digest preimages (`module_engineer\0${engineer_id}`, `parts.join('\0')`), so `grep`/`rg` classify those files as binary and skip them without printing a match and without a visible error. Re-reading the same files byte-level with `python3` immediately produced the hits.
- Mistake pattern: reading "no grep output" as "the symbol is not there". A binary-classified file is silently excluded from the result set, so an audit over a directory that contains one reports a confident absence over a smaller corpus than the one it claims to have searched.
- Prevention rule: before concluding a symbol is absent from a module, confirm the corpus. When any file under audit can carry raw control bytes — NUL-joined digest preimages, canonical byte builders, checksum separators — search it with a byte-level tool (`python3`, `od -c`, or `grep -a`) rather than plain `grep`/`rg`, and treat an empty result over such a directory as unproven until the file list is verified.
- Where to apply next time: every audit or symbol sweep touching `src/core/collaboration/**` and any other module whose canonicalization joins parts on a control character; also any "the field does not exist anywhere" claim used as evidence in a review.

- Date: 2026-08-30
- Triggered by correction: the C3 ship gate blocked at `verify-sprint --prepare-acceptance` with `unresolved-major-change` / `verified-flow-proof-changed`, after `bash scripts/check-architecture-sync.sh` had reported `blocking=0 pending=0 uncommitted=0` on the same tree four separate times across two rounds. The contract's `commands_succeed` named only that script, so every pre-gate round read green. The script reports queue counters — how many drift requests are pending and whether the projection job queue is drained — and never computes a projection. The real state was that `capability.runtime-harness.collaboration` had been unprovable since the record-store extraction, because the capability node and `flow.collaboration.publish-signal.yaml` still named `resolveModuleEngineerActor` and `readPersistedSignal`, two symbols the extraction had moved out of `signal-store.ts`.
- Mistake pattern: treating a queue-drain counter as proof that the projection can apply. Nothing in the queue view runs the flow-proof compiler, so a model whose selectors point at deleted symbols reports a perfectly drained queue right up to the gate that actually projects. The same shape bites whenever a cheap status command and the real gate read different state.
- Prevention rule: any work-package that moves, renames, or extracts a symbol under a capability's `source.include` must run a real projection — `repo-harness architecture-projection check --json` or `repo-harness run verify-sprint --prepare-acceptance` — as its own exit criterion, not `check-architecture-sync.sh` alone. Grep the capability node and every `.archcontext/model/flows/*.yaml` for the moved symbol names in the same commit that moves them; a flow selector is a source reference with no compiler behind it. When the projection does refuse, read `refreshSignals[0].reasonCodes` for the real classification: `humanActions[].reasonCode` only ever says `unresolved-major-change`.
- Where to apply next time: every extraction or rename inside `src/core/**` or `src/effects/**` that a capability node declares, and every contract whose exit criteria currently cite `check-architecture-sync.sh` as its only architecture gate.

- Date: 2026-08-30
- Triggered by correction: C4 added one archcontext relation and one flow, both required for the capability's P2 proof, and `tests/architecture-projection-e2e.test.ts` went red on `expect(relations).toHaveLength(39)` and `expect(flows).toHaveLength(25)`. C3 hit the same pin one row earlier on the flow count alone. Neither the contract's exit criteria nor `verify-sprint --prepare-acceptance` runs that file, so the model change reads green through every gate the contract names and only fails when the architecture suite is run explicitly.
- Mistake pattern: an inventory pin that lives outside the gate it constrains. AXR7 asserts exact counts over `.archcontext/model/**` so that model growth is deliberate, which is the right intent, but the gate that would surface it is not part of the architecture acceptance path. A row can therefore land an accepted, proven projection and still leave a red repository.
- Prevention rule: any work-package that adds or removes a file under `.archcontext/model/nodes`, `relations`, or `flows` must run `bun test tests/architecture-projection-e2e.test.ts` in the same round as the projection acceptance, and move the AXR7 counts in the same commit as the model file. Declare `tests/architecture-projection-e2e.test.ts` in the contract's `allowed_paths` up front rather than amending after a refusal.
- Where to apply next time: every row in the collaboration program from C5 onward, and any capability that gains an entrypoint, a relation, or a flow.

- Date: 2026-08-30
- Triggered by correction: diagnosing why a valid `acceptedChange` was being discarded needed the flow-proof diagnostics, so `node_modules/archctx/bin/archctx.mjs` was patched to dump them. The patched build produced no output at all, and the next projection run failed with `AC_RUNTIME_VERSION_UNSUPPORTED` naming a running `archctxd` pid. The projection does not execute the file on disk: a long-lived daemon started from the pre-patch bytes serves the request, and the version handshake between CLI and daemon is what surfaces the divergence.
- Mistake pattern: assuming a CLI entrypoint under `node_modules` is the code that runs. When a tool ships a daemon, editing its bundle is a no-op until the daemon is restarted, and the failure mode is a silent no-op followed by a confusing version error rather than an obvious "stale process" message.
- Prevention rule: when instrumenting or patching `archctx` for diagnosis, run `node node_modules/archctx/bin/archctx.mjs daemon stop` after every edit and before the next projection command, and restore the pristine bytes plus verify with `shasum -a 256` against a pre-patch copy before recording any acceptance evidence. The same applies to any vendored tool with a `daemon start|status|stop` surface.
- Where to apply next time: any archctx flow-proof or projection diagnosis, and any future instrumentation of a vendored dev dependency that runs as a background service.

- Date: 2026-08-30
- Triggered by correction: a C4 implementation commit hand-edited the sprint backlog row for its own task, flipping `[ ]` to `[x]` and writing a plan pointer into the Plan column. Row state is `finish`'s output, not the implementer's: `repo-harness run sprint-backlog complete-task` rejects a row that is already complete, and because the completion runs inside one transaction the refusal rolls the entire finish back, taking the archive moves and the pointer rewrite with it.
- Mistake pattern: treating a status table as documentation to keep current while working, when it is the write surface of a later transaction. The hand-edit looks harmless and reads correct, so nothing surfaces until finish refuses on a row that already says what finish was about to write. The telltale on inspection is a `[x]` row with no Execution Log line and a Plan pointer into live `plans/` rather than `plans/archive/`.
- Prevention rule: an implementation commit never writes a sprint backlog row's Status or Plan cell. Leave the row `[ ]` with `(pending)` and let `contract-worktree finish` / `sprint-backlog complete-task` write both. If a row was already hand-edited, restore it to its pre-edit state in the same commit that discovers it, before running finish; do not attempt to make finish accept the edited row.
- Where to apply next time: every row of the collaboration program, and any contract worktree whose `allowed_paths` includes a `plans/sprints/*.sprint.md` file for reasons other than the row's own completion.

- Date: 2026-08-30
- Triggered by correction: C6 declared nine archcontext entrypoints, one relation and one flow, and the orchestrator-approved `acceptedChange` was silently discarded — `apply` returned `human-action-required` with the acceptance missing from `refreshSignals[0]` and `diagnostics: null`. The cause was in the new flow: eleven steps were written `from: collaboration to: collaboration`, and a capability-to-itself step has no relation to bind, so each produced `relation-binding-missing`, P2 came back `unprovable`, and `classifyArchitectureMajorChange` drops a valid `acceptedChange` whenever any capability is unprovable. The existing collaboration flows already route intra-capability steps through the `component.collaboration.primary` participant; matching them made P2 `proven` and the same acceptance landed unchanged.
- Mistake pattern: writing an intra-capability flow step as a self-edge because it reads naturally in prose. The refusal that follows names only `unresolved-major-change` and carries no diagnostic, so the visible symptom is "the approval did not take" rather than "the model does not compile", which sends the next round at the acceptance mechanics instead of at the flow.
- Prevention rule: every `archcontext.flow/v1` step whose work stays inside one capability must target that capability's component participant (`component.<capability>.primary`), never the capability itself; only cross-capability steps name another capability, and each needs a declared relation. Before spending an approval event, confirm the model compiles: no capability may be `unprovable`, since an unprovable capability discards the acceptance rather than reporting it. `refreshSignals[0].reasonCodes` is the real classification and still says nothing about proof status, so the compiler diagnostics are the only source — instrument `compileSemanticCapabilityDiagrams()` in a throwaway `node_modules` patch, then revert and verify with `shasum -a 256`.
- Where to apply next time: every new `.archcontext/model/flows/*.yaml` in the collaboration program from C7 onward, and any capability gaining a flow whose steps are mostly internal.

- Date: 2026-08-30
- Triggered by correction: C7's new flow silently discarded an orchestrator-approved `acceptedChange` twice, with the same signature C6 recorded for self-edges but two different causes. First, a step whose evidence edge was `collect -> collectEngineerOffers` where the call actually sat inside the `read_execution_offers: () => ...` callback the collector requires; the proof follows direct statements, not calls through a closure. Second, a step crossing from the collaboration capability to `capability.runtime-harness.engineer-scheduling` with no `relation.collaboration.engineer-scheduling` declared. Each left the capability `unprovable`, which drops the acceptance and reports only `unresolved-major-change`.
- Mistake pattern: assuming a flow step is provable because the two symbols really do call each other in the source. The proof needs a direct call site and, across capabilities, a declared relation; a callback boundary or a missing relation file breaks it with no diagnostic. Retrying compounds it — archctx dedupes on `changeSetId`, so a second attempt with the same id returns `noop` without writing, and the durable apply receipt from a bisecting run will deliver stale refresh signals that make a failed classification look like it succeeded.
- Prevention rule: before spending an approval event on a new flow, check each step's `sourceSymbol` calls its `sinkId` symbol as a direct statement — extract a named module-level function when the real call is inside a callback or an `.action()` handler, keeping the callback so runtime semantics are unchanged — and confirm a relation exists for every cross-capability step. When bisecting a refusal, `git checkout -- docs/architecture` and use a fresh `changeSetId` for each attempt, and treat any "durable apply receipt reconciled" message as evidence that the run replayed an earlier receipt rather than classifying.
- Where to apply next time: every remaining collaboration-program flow, and any capability gaining a flow whose steps cross into another capability or run through commander/`.action()` handlers.

- Date: 2026-08-30
- Triggered by correction: C7's `collaboration handoff list` called `listWorkStateHandoffs()` raw and returned each record's `execution_context` verbatim, while the sibling read three functions above it routed through `collectCollaborativeWorkExchange()`. `publishWorkStateHandoff()` validates a `bound_task` branch for shape only — the author supplies the Claim id, the Lease generation and the freeze digest — so the unverified list handed an agent exactly the unproven Claim C5's read-time proof exists to withhold. This is the same leak C6 removed from its own collection one row earlier, re-opened on the first agent-facing surface, and the fifth occurrence in this program of the guarded-main-path/unguarded-sibling motif.
- Mistake pattern: writing a "list everything" read next to a projected read and treating the store's own list function as the obvious source. The raw reader is exported, validates each record against its digest, and returns successfully, so nothing about the call site looks unsafe; the missing thing is a proof that lives one layer up, in a projection the sibling read already goes through. A guarded main path makes the motif harder to see, not easier, because the guard is visibly present in the file.
- Prevention rule: for every read on an agent-facing or externally-visible surface, answer one fixed question before writing it — *what is this data's existing verified projection, and did I route through it?* Route through it, or adjudicate the exception in a comment at the read naming the field set that makes the raw read safe. Then make it structural: do not import the raw list/record readers into the surface module at all, and pin that import set in a test, so the unverified path is unreachable rather than merely unused. Prove the fix with a forged-record non-containment test (publish a structurally valid record carrying a forged unverifiable branch, assert the serialized payload of every read surface contains none of its values, and assert the record's honest fields still project).
- Where to apply next time: C8's Operator read surface and C9's canary reads, and any future row that adds a read to `agent-surface.ts`, the MCP tool set, or an Operator route.

- Date: 2026-08-30
- Triggered by correction: after C7 routed `handoff list` through the verified Work Exchange, the second review found the same unproven `execution_context` still escaped through `handoff publish`'s success acknowledgement. The write path returned the complete persisted `WorkStateHandoffV1`; because `publishWorkStateHandoff()` proves shape and persistence but not the caller-supplied `bound_task` authority, the acknowledgement looked byte-for-byte like a verified handoff even though it had never crossed C5's read-time proof. The read sweep was green while an adjacent write egress violated the same invariant for the sixth time in the program.
- Mistake pattern: classifying safety by operation name (`read` versus `write`) instead of serialized data shape. A mutation response is also a read surface for its caller, and returning a domain record from a successful write silently claims every field in that record was established by the write. Persistence, schema validity and semantic proof are distinct claims.
- Prevention rule: inventory every externally serialized egress — list/read responses, mutation acknowledgements, events, logs and error payloads — by field set. A value may appear only if that exact boundary proved its meaning; otherwise return an identity-only acknowledgement and require consumers to enter through the verified projection. Pin the acknowledgement shape at every transport and include it in the same forged-value non-containment test as the named read surfaces.
- Where to apply next time: C8 Operator actions, C9 canary evidence, and any future CLI/MCP mutation that returns a collaboration, Task, Lease, Publication or Acceptance record carrying fields whose semantic proof lives above the store.

### 2026-09-05 — Generated host guidance verification includes output parity

When changing `assets/partials/` or `assets/partials-agents/`, run
`bun test tests/assembly.test.ts tests/agents-assembly.test.ts tests/output-parity.test.ts`
as the focused generated-output boundary. Host-specific assertions alone miss
cross-target command contracts and aggregate line budgets. Synchronize intentional
command changes with parity assertions, and shorten source guidance instead of
raising the existing line limit. Reproduced on `0d6bc102`: 27 pass / 2 fail;
repair: 58 pass across the three named files, AGENTS restored to 260 lines.

### 2026-09-05 — A red main hides every later CI failure
- Date: 2026-09-05
- Triggered by correction: three consecutive `main` repairs (#319, #321) each exposed the next failure only after the previous one was fixed.
- Mistake pattern: `check:ci` runs test files one by one and exits at the first failing file, so a single stale golden or fixture masks unrelated regressions landed later. Branch CI runs on the merge result, so a PR inherits every masked failure on `main`.
- Prevention rule: before opening or rebasing a PR while `main` is red, run the full local suite on `origin/main` (or on the rebased branch) once and fix or report every failing file in one PR; do not chase CI one failing file at a time. A commit that changes guidance text, hook call shapes, or partials must refresh its characterization goldens and parity assertions in the same commit.
- Where to apply next time: any repair whose CI reason is "the first failing file"; use `bun test --timeout 60000 > <log> 2>&1; echo FULL_EXIT=$?` and read every `(fail)` line before dispatching a fix.

### 2026-09-05 — The task-sync digest only binds while the branch carries main's tip
- Date: 2026-09-05
- Triggered by correction: a PR with a correctly computed `check-task-sync` digest went red in CI after `main` moved, and re-running the local check against the working tree produced a digest CI still rejected.
- Mistake pattern: treating the digest as a property of the diff alone. It hashes the resolved base sha together with the raw diff, and CI resolves that base against the PR merge ref, so the value binds only while the branch already contains `main`'s tip. `REPO_HARNESS_DIFF_BASE=origin/main REPO_HARNESS_DIFF_MODE=merge-base` reproduces CI exactly and only while the branch point still equals `main`'s tip; every commit that lands on `main` in the meantime silently invalidates a digest that was correct when it was written.
- Prevention rule: compute the digest with `REPO_HARNESS_DIFF_BASE=origin/main REPO_HARNESS_DIFF_MODE=merge-base bash scripts/check-task-sync.sh` immediately before pushing, and after any movement on `main` bring the branch forward with `git merge origin/main` and rebind in the same round. Do not rebase to refresh the base — the digest tracks the resolved base sha, not the commit topology, and a rebase discards the merge history the reviewer reads.
- Where to apply next time: every PR whose changed set includes workflow artifacts under `tasks/` or `plans/`, and any long-lived branch opened while `main` is moving.

### 2026-09-05 — A per-file test loop under `set -e` must aggregate, not stop at the first file
- Date: 2026-09-05
- Triggered by correction: `scripts/lib/ci-run-tests.sh` ran each test file in a loop under `set -e`, so the first failing file ended the run and CI reported it as the only failure. Across several `main` commits on 2026-09-05 the red set changed three times without anyone seeing it, and each repair exposed the next masked failure only after the previous one landed.
- Mistake pattern: a fail-fast gate presented as a full-suite result. The run looks authoritative — it iterates the whole discovered set — but the exit under `set -e` truncates it, so a single stale golden hides every regression behind it, and a branch that merges onto that `main` inherits the masked failures as its own CI signal.
- Prevention rule: a per-file loop in a CI gate records each file's exit code, continues, and fails once at the end with the complete list of failing files. Guard the loop body against `set -e` explicitly (`|| status=1`) rather than relying on the loop to keep going, and assert the aggregate behavior in a test so the gate cannot silently regress to fail-fast.
- Where to apply next time: any shell gate that iterates a discovered set of files, tests, or repositories — `scripts/lib/ci-run-tests.sh` and the checks under `scripts/check-*.sh` that fan out over paths.
# Approved plan edit readiness (2026-09-07)

Planning approval and execution completion are separate observations. An approved plan with open tasks must be editable; shipping still requires completion and fresh evidence. Missing plan text cannot prove either. Regression and source rationale: `docs/researches/2026-09-07-approved-plan-edit-readiness.md`.
