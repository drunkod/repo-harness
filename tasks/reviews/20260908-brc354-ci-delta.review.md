# BRC354 CI delta review

> **Substantive Change SHA256**: `sha256:0a6f6a78cf44a8e34135938f5459958887996253f1a7c043e977d148fb4ea0f4`

Scope: six-file delta after `cb0967fe`; the archived acceptance remains bound to its original subject.

## Root Cause Evidence

- Observed failure: CI run 34208739336 failed 19 lifecycle/closeout assertions at the trusted-runtime mount guard on Linux.
- Concrete cause: Linux fixtures placed repositories and harness homes under `/tmp`, which is reserved as container-owned tmpfs. macOS fixtures used a different canonical temporary root.
- Correction: only containment fixtures select `/var/tmp` on Linux; ordinary adoption fixtures retain their default. Production mount restrictions are unchanged.
- Regression evidence: the three failing test files pass in a credential-free Linux container (37 pass, 1 skip, 0 fail). The skipped Docker handoff case retains its original immutable lifecycle baseline; this run does not claim to repeat it.

## Authorized redaction refinement

Declared path-array entries still preserve extensionless paths, but a high-entropy token in any segment now takes the existing redaction pass. The Owner confirmed integration of the parallel edit. All six declared collections are tested through the actual ledger writer without a supplied secret list.

## Verification

- `bun test tests/evidence-event-store.test.ts tests/evidence-projection-drift.test.ts`: 43 pass, 0 fail.
- Linux Bun 1.4.0: `tests/effects/brc10-lifecycle.test.ts`, `tests/effects/campaign-closeout.test.ts`, `tests/campaign-finish-failure-audit.test.ts`: 37 pass, 1 skip, 0 fail. Test environment adds jq and executable disposable tmpfs; network disabled, repository read-only, unprivileged UID, no credentials or Docker socket.
- TypeScript and required repository integrity checks run for this delta. Required CI on the pushed head remains the merge gate.
- No production containment code changed in this delta; original Docker execution evidence `vx-c16e467c5e8849cd8928` and lifecycle evidence `vx-a9feb18d252440e5a024` retain their original subjects. No new local full-suite claim.

Production active admission remains closed. No real model calls, active campaign, release, global installation, or change to BRC6a Owner closeout occurred. BRC14/BRC15 real acceptance remains outstanding.

## Takeover evidence readback

- Integrated head: `22b9032ca7d4a390215c93d63732d5742baa1f01`. The parallel redaction patch is byte-identical to isolated candidate 9b06d83e; the integrated fixture choice is Linux /var/tmp. The isolated home-based variant is superseded and is not a second active task.
- Actual Linux log: `/tmp/brc354-linux-final.log` (37 pass, 1 skip, 0 fail, 272 assertions, 36.50 seconds). The skipped test is `expired Docker handoff reconciles one charge without a terminal or a new owner`.
- Baseline events were read from the accepted original ledger and checked against their run-file hashes: Docker `vx-c16e467c5e8849cd8928`, sha256:f5a36a4e33a46bb1efd8689cee1d7a0abac3978ca87dd352f28d2f43fa8c213d; lifecycle `vx-a9feb18d252440e5a024`, sha256:ab6ad3967650935148b8379eb8c01147b8758ac4c39c8fbd9a2504c47e4dcf55. These remain historical records from the original tasks/contracts path.
- The separate disposable checkout's archived-contract re-verification failed baseline-path lookup and architecture state. No new AcceptanceReceipt was issued there, and its failed prepared logs remain retained. This delta review does not relabel the archived receipt or claim to complete all #354 recovery boundaries.
- Remaining independent acceptance: controller loss before durable invocation publication and a recovery-compatible retention/cleanup contract. Durable scope is recorded in docs/researches/2026-09-08-brc354-independent-supervision.md.
