> **Archived**: 2026-08-12 21:59
> **Related Plan**: plans/archive/plan-20260812-2118-release-0-15-0.md
> **Outcome**: Completed
> **Lifecycle**: contract
> **Parent Run ID**: run-20260812-2159

# Task Contract: release-0-15-0

> **Status**: Fulfilled
> **Plan**: plans/plan-20260812-2118-release-0-15-0.md
> **Task Profile**: code-change
> <!-- legal values: code-change | docs-only | ledger-closeout | migration | eval-only | delegated-run | bugfix (omit for legacy passthrough); see docs/reference-configs/sprint-contracts.md -->
> **Owner**: kito
> **Capability ID**: root
> **Last Updated**: 2026-08-12 21:18
> **Review File**: `tasks/reviews/20260812-2118-release-0-15-0.review.md`
> **Notes File**: `tasks/notes/20260812-2118-release-0-15-0.notes.md`
> **Exemplar**: `docs/reference-configs/contract-brief-example.md`

## Why

`v0.14.2..main` contains one backward-compatible hook capability plus its
authority cutover and a cursor-retention bug fix. Publishing without a coherent
minor-version candidate would either misclassify the public change or allow the
npm version, downstream workflow stamp, documentation, packed hook bundle, and
consumer proof to drift.

## Goal

Prepare, verify, merge, and push the exact `repo-harness@0.15.0` candidate while
leaving npm publication, `v0.15.0`, the GitHub Release, and the selected global
installation untouched for a later explicit release action.

## Scope

- In scope: release version authorities/projections, five README current-release
  lines, AXR7 packed-version assertion, changelog, release filing, packed hook
  bundle, the directly blocking leaderless-provider cleanup test assertion, and
  workflow-owned projections needed to close this contract.
- Out of scope: product behavior changes, dependency changes, compatibility
  paths, historical release files, the unrelated Oracle fixture string, npm
  publish, tag/GitHub Release creation, and global installation mutation.
- Taste constraints: keep the release diff mechanical and auditable; each
  changelog claim must map to a commit in `v0.14.2..candidate`.

## Stop Conditions

- Stop and hand back to the parent if the change would require editing a path outside Allowed Paths.
- Stop if an Exit Criteria command cannot be run in this environment.
- Stop if Goal, Scope, or Exit Criteria are internally contradictory.

## Falsifier

If the current version checker or packed tarball readback proves another live
version authority, the enumerated surface is incomplete and the contract must
be widened before editing it. Cheapest proof: run `check-skill-version`, inspect
all live `0.14.2` matches, and compare the 0.14.2 release diff shape before the
candidate gate.

## Root Cause Evidence

Required when Task Profile is `bugfix`; leave as-is otherwise.

- root_cause: one sentence naming file:line/condition (testable, not "a state issue").
- repro: the command or UI path that reproduces the symptom.
- regression_guard: path to a test that fails on the unfixed code and passes after the fix (must also appear under exit_criteria.tests_pass).
- pre_fix_failure_artifact: path to a captured run of regression_guard on the UNFIXED code. Capture with `bun test <regression_guard> > <artifact> 2>&1; echo "PRE_FIX_EXIT=$?" >> <artifact>` (no pipes — pipes swallow the exit status). The gate requires a non-zero `PRE_FIX_EXIT=` line plus the regression_guard path string in the artifact (see the Root Cause Evidence Gate section in docs/reference-configs/sprint-contracts.md).

## Workflow Inventory

- Source plan: `plans/plan-20260812-2118-release-0-15-0.md`
- Deferred-goal ledger: `tasks/todos.md`
- Review file: `tasks/reviews/20260812-2118-release-0-15-0.review.md`
- Notes file: `tasks/notes/20260812-2118-release-0-15-0.notes.md`
- Checks file: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope gate: edit only paths listed under `allowed_paths`; update this contract before widening scope.
- Completion gate: run `verify-sprint --prepare-acceptance`, record one typed AcceptanceReceipt under the frozen policy below, then run `verify-sprint`; review Markdown is projection only.

## Acceptance Policy

```json
{"protocol":1,"reviewer":"Claude","user_waiver":"allowed"}
```

## Allowed Paths

```yaml
allowed_paths:
  - package.json
  - assets/skill-version.json
  - README.md
  - README.es.md
  - README.fr.md
  - README.ja.md
  - README.zh-CN.md
  - scripts/axr7-consumer-e2e.ts
  - tests/unit/closeout-runner-guardrails.test.ts
  - dist/hook-entry.js
  - docs/CHANGELOG.md
  - deploy/release-checklists/260812-repo-harness-0.15.0.md
  - docs/architecture/
  - .archcontext/
  - plans/
  - tasks/todos.md
  - tasks/current.md
  - tasks/contracts/20260812-2118-release-0-15-0.contract.md
  - tasks/reviews/20260812-2118-release-0-15-0.review.md
  - tasks/notes/20260812-2118-release-0-15-0.notes.md
```

## Evidence Requirements

```yaml
evidence_requirements:
  # Set benchmark to required when this contract consumes the harness profile benchmark matrix.
  benchmark: not_applicable
```

## Delegation Contract

```yaml
delegation:
  budget:
    tokens: null
    runner_invocations: null
    wall_time_minutes: null
  permission_scope:
    mode: inherit_allowed_paths
    writable_paths: []
    network: inherited
  roles:
    parent:
      mode: narrate_and_gatekeep
      purpose: approval_checkpoint_owner
    explorer:
      mode: read_only
      purpose: codebase_research
    worker:
      mode: edit_within_allowed_paths
      purpose: implementation
    verifier:
      mode: read_only
      purpose: exit_criteria_review
  runner:
    preferred:
      - subagent
    fallback: null
    brief_is_authoritative: true
```

## Exit Criteria (Machine Verifiable)

```yaml
exit_criteria:
  files_exist:
    - package.json
    - assets/skill-version.json
    - docs/CHANGELOG.md
    - deploy/release-checklists/260812-repo-harness-0.15.0.md
  artifacts_exist:
    - .ai/harness/checks/latest.json
    - .ai/harness/runs/release-0.15.0-check-release.log
    - .ai/harness/runs/release-0.15.0-check-release.result.json
    - tasks/notes/20260812-2118-release-0-15-0.notes.md
  tests_pass:
    - path: tests/skill-version.test.ts
    - path: tests/unit/hook-entry-single-file-bundle.test.ts
  commands_succeed:
    - bun scripts/check-skill-version.ts
    - bun test tests/skill-version.test.ts tests/unit/hook-entry-single-file-bundle.test.ts
    - git diff --quiet f3b4afe4da639e59927af542ed7f6359601e6e3d -- package.json assets/skill-version.json README.md README.es.md README.fr.md README.ja.md README.zh-CN.md scripts/axr7-consumer-e2e.ts tests/unit/closeout-runner-guardrails.test.ts docs/CHANGELOG.md && test "$(sha256sum .ai/harness/runs/release-0.15.0-check-release.log | awk '{print $1}')" = "2ae06390ad93fd756bae083f630135f59584647419ac6c76f3646112a03257f8" && jq -e '.exit_code == 0 and .tests.pass == 2364 and .tests.skip == 1 and .tests.fail == 0 and .tarball_smoke == "pass" and .npm_unpublished == true' .ai/harness/runs/release-0.15.0-check-release.result.json >/dev/null
    - bash scripts/check-deploy-sql-order.sh
    - bash scripts/check-architecture-sync.sh
    - bash scripts/check-task-sync.sh
    - repo-harness run check-task-workflow --strict
    - bun scripts/inspect-project-state.ts --repo . --format text
    - bun src/cli/index.ts init --repo . --dry-run
```

## Acceptance Notes (Human Review)

- Functional behavior: version and release projections agree; the packed
  tarball reports 0.15.0 and starts through both public bins.
- Edge cases: coincidental third-party/fixture `0.14.2` strings remain unchanged;
  no public release state is claimed before readback.
- Regression risks: Stop fan-out on very large dirty trees remains the recorded
  deferred pressure point; no new behavior is introduced in this slice.

## Rollback Point

- Commit / checkpoint: frozen release-prep candidate before merge to `main`.
- Revert strategy: before publish, revert or abandon the candidate; after
  publish (outside this slice), never move/reuse 0.15.0 and fix forward.
