> **Archived**: 2026-08-12 21:59
> **Related Plan**: plans/archive/plan-20260812-2118-release-0-15-0.md
> **Outcome**: Completed
> **Lifecycle**: notes
> **Parent Run ID**: run-20260812-2159

# Implementation Notes: release-0-15-0

> **Status**: Active
> **Plan**: plans/plan-20260812-2118-release-0-15-0.md
> **Contract**: tasks/contracts/20260812-2118-release-0-15-0.contract.md
> **Review**: tasks/reviews/20260812-2118-release-0-15-0.review.md
> **Last Updated**: 2026-08-12 21:18
> **Lifecycle**: notes

## Design Decisions

- Release as `0.15.0`, not `0.14.3`, because `v0.14.2..main` contains a `feat`
  commit and the `/check` ship rule maps a backward-compatible feature to minor.
- Treat `package.json.version` and both `assets/skill-version.json` fields as
  the enforced authority set. The READMEs and AXR7 constant are projections;
  `.claude/.skill-version` is an older repository stamp and the Oracle `0.14.2`
  test value is a third-party fixture, not a release authority.
- Stop this work-package before npm publish, tag creation, GitHub Release, or
  global install mutation. Those irreversible/external actions require a later
  explicit authorization and exact-main CI evidence.

## Deviations From Plan Or Spec

- The first full release gate found one unrelated flaky assertion in
  `tests/unit/closeout-runner-guardrails.test.ts` (2363 pass, 1 skip, 1 fail).
  The focused test immediately passed. Inspection proved the assertion raced
  contender lock acquisition against producer exit, even though production
  releases the token after provider-group drain and then calls `process.exit`.
  Scope was widened for this single release-blocking test repair; production
  code remains unchanged.
- The corrected invariant passed 10 consecutive focused runs and the complete
  `closeout-runner-guardrails` file passed 24/24.

## Tradeoffs Considered

| Option | Decision | Reason |
|--------|----------|--------|
| Patch `0.14.3` | Reject | The release range contains a backward-compatible feature. |
| Minor `0.15.0` | Use | Matches the repository ship rule and public capability change. |
| Prepare and publish in one slice | Reject | It crosses the reversible candidate boundary before user review. |

## Open Questions

- None.

## Evidence Links

- Checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Version authority: `bun scripts/check-skill-version.ts` passed with package,
  skill, and template all at 0.15.0.
- Targeted tests: 19 pass / 0 fail across `tests/skill-version.test.ts` and
  `tests/unit/hook-entry-single-file-bundle.test.ts` after the isolated
  worktree reused the source checkout's installed dependencies.
- Package dry-run: `repo-harness@0.15.0`, 476 files, 9,893,134 packed bytes,
  integrity `sha512-tf7aOsbfTcplYB4BUWEwZUfqVEQSqDXIk40XtRVKY3/GZ2IbnpvOmbraRgqlmRf9yXUsST1lQxZRwmWV7LpzXA==`.
- Pre-freeze repository checks: typecheck, architecture sync, task sync,
  strict task workflow, project inspection, and init dry-run passed.
- Final frozen-product release gate: `bun run check:release` at product commit
  `f3b4afe4da639e59927af542ed7f6359601e6e3d` passed with 2364 tests, 1
  platform skip, 0 failures, and 18,172 expectations across 183 files; npm
  unpublished lookup, tarball clean install, CLI/hook bin startup, and all CI
  gates passed. The 326,071-byte log is cached at
  `.ai/harness/runs/release-0.15.0-check-release.log` with SHA-256
  `2ae06390ad93fd756bae083f630135f59584647419ac6c76f3646112a03257f8`.
- Contract closeout reuses that expensive evidence only while every release
  product path remains byte-identical to `f3b4afe4`; workflow accounting files
  may advance without rerunning the release gate.
- The release filing and workflow artifacts are operational evidence rather
  than npm package inputs; they may project the verified result after the
  frozen product commit without invalidating its package/test evidence.

## Promotion Filter

Promote a candidate to `tasks/lessons.md`, `docs/researches/`, or harness asset files only when all three hold: hard to reverse, surprising without local context, and a real trade-off existed. If any one is missing, keep it in this notes file instead.

## Promotion Candidates

- Promote to `tasks/lessons.md` only after a repeated correction or failure pattern.
- Promote to `docs/researches/` only when it is durable repo knowledge with evidence.
- Promote to harness asset files only after verification across more than one task or fixture.
