> **Archived**: 2026-08-20 05:10
> **Related Plan**: plans/archive/plan-20260820-0356-release-0-16-0.md
> **Outcome**: Completed
> **Lifecycle**: review
> **Parent Run ID**: run-20260820-0510

# Task Review: release-0-16-0

> **Status**: Complete
> **Plan**: plans/plan-20260820-0356-release-0-16-0.md
> **Contract**: tasks/contracts/20260820-0356-release-0-16-0.contract.md
> **Notes File**: tasks/notes/20260820-0356-release-0-16-0.notes.md
> **Checks File**: .ai/harness/checks/latest.json
> **Last Updated**: 2026-08-20 04:50
> **Recommendation**: pass
> **Review Rubric Version**: 2
> **Reviewed Subject SHA256**: sha256:ed7b43c54ab4ade52944e421d55422f416797e192e4af23436fa7dd2ba8db4bf
> **Reviewed Subject Scope**: normalized-final-content
> **Reviewed Target Revision**: 37b7cada9f6d9b54d034b0391e0afb25432ab56b

## Human Review Card

- Verdict: PASS
- Change type: docs-only
- Intended files changed: release metadata only — `package.json` version, `assets/skill-version.json` (version + templateVersion + history entry), the five READMEs' current-release block, `scripts/axr7-consumer-e2e.ts#REPO_HARNESS_VERSION`, the `0.16.0` CHANGELOG section, the release filing, and the plan/contract/notes/review/todos workflow artifacts
- Actual files changed: 15 files, +806 -15; every path inside `allowed_paths` (`allowed_paths_check.outside` empty). Zero product code: the only `scripts/` hit is the axr7 version string, and nothing under `src/` or `assets/templates/` is touched
- Commands passed: `bun run check:type` (exit 0), `bash scripts/check-deploy-sql-order.sh` (exit 0), `bash scripts/check-task-sync.sh` (exit 0), standalone `bun test` (2702 pass / 1 skip / 1 fail / 20536 expect across 199 files, the single failure reviewer-induced and green 6/6 in isolation), `npm pack --dry-run --json` (502 entries, shasum `76e601adab53a2a93104b70bb4e8b45d81d26924`, 10077287 packed, 15425182 unpacked)
- Residual risks: three findings on the receipt (one P2, two P3), none blocking. The P2 is a pre-existing harness defect already on `main`, ledgered in `tasks/todos.md`, not reachable from this candidate's diff. Both P3s are closed: one is the filing correction this gate forced, the other is a ledgered flake observation
- Reviewer action required: none for acceptance. Publication, tag, GitHub Release, and merge to `main` remain the orchestrator's follow-on phase and are explicitly out of this contract's scope
- Rollback: before publication, abandon or revert `codex/release-0-16-0`; nothing outside this repository has changed. Rollback point is `main@37b7cada9f6d9b54d034b0391e0afb25432ab56b`

## Mode Evidence

- Selected route: gatekeeper final-subject review of the release candidate on `codex/release-0-16-0`, returning FAIL on the first pass, then re-review of the orchestrator's fix followed by the explicit receipt-closure order. Both review dispatches carried zero side effects; the correction commit `6391e1dd` was authored by the orchestrator after the gate returned a structured finding
- P1/P2/P3 evidence: P1 the release surface splits into version anchors (`package.json`, `assets/skill-version.json`, five READMEs, `scripts/axr7-consumer-e2e.ts`), the human-readable changelog (`docs/CHANGELOG.md`), the audit filing (`deploy/release-checklists/`), and the workflow artifacts (`plans/`, `tasks/`); no runtime code participates. P2 traced the version datum end to end — `package.json#version` to `assets/skill-version.json#version`/`templateVersion` to the generated workflow stamp quoted in all five READMEs to the axr7 consumer e2e constant to the packed tarball — and confirmed `npm pack` emits `repo-harness-0.16.0.tgz` at the filing's exact entry count, packed size, and shasum. The same trace is what surfaced the unpacked-size discrepancy, since that was the one number in the chain that did not reconcile. P3 the load-bearing decision is version selection, and `0.16.0` over `0.15.4` is justified and recorded: the 0.15.3 filing reserved this minor in writing for the release where WP2 and WP3 ship together, both landed in `v0.15.3..37b7cada`, and the consumer-visible surface grows by a new CLI verb plus two hook behaviors
- Root cause or plan evidence: not applicable — Task Profile is `docs-only`, no bugfix root cause is required. The release scope classification is recorded in `plans/plan-20260820-0356-release-0-16-0.md` and the filing's Release scope section

## Verification Evidence

- Waza `/check` run: `repo-harness run verify-sprint --prepare-acceptance` — `total=9 failed=0 status=Fulfilled`, `Sprint verification passed`, evidence materialized `status pass`; closed with `repo-harness run verify-sprint` reporting `Sprint acceptance finalized without rerunning verification`
- Commands run: `bun run check:type` exit 0 (952ms, no diagnostics); `bash scripts/check-deploy-sql-order.sh` exit 0 (49ms); `bash scripts/check-task-sync.sh` exit 0 (40ms); standalone `bun test` with `REPO_HARNESS_SOURCE_ROOT` unset produced 2702 pass / 1 skip / 1 fail / 20536 `expect()` across 199 files in 610.04s, and the sole failure `tests/runtime-evidence-receipt.test.ts` re-ran 6 pass / 0 fail in 3.62s isolated
- Manual checks: independently recomputed every number the filing asserts rather than trusting it. `npm view repo-harness version` returned `0.15.3` and `npm view repo-harness@0.16.0 version` returned 404, matching the recorded baseline. `npm pack --dry-run --json` reported 502 entries, shasum `76e601adab53a2a93104b70bb4e8b45d81d26924`, 10077287 packed and 15425182 unpacked; a real `npm pack` produced a tarball with the identical shasum, and extracting it confirmed 502 files whose per-file `stat` sum is 15425182. The filed unpacked value of 15809024 reconciled with no measurement method and was returned as a blocking finding before any receipt bound; `6391e1dd` corrected it and this gate re-verified the corrected line against a fresh `npm pack`. Anchor sweep found no stale `0.15.3` outside historical references (the `assets/skill-version.json` history entry, the prior filing, `tasks/lessons.md`, and this cycle's own plan/contract/notes). Every `0.16.0` CHANGELOG bullet was traced to a real commit in `v0.15.3..37b7cada` (8 commits confirmed by `git rev-list --count`) and each claimed identifier was confirmed present in `src/` rather than taken from prose. History is clean: three release-prep commits plus the correction commit, all authored by the user, with substantive message bodies and no AI attribution trailers
- Supporting artifacts: `.ai/harness/checks/latest.json`, `deploy/release-checklists/260820-repo-harness-0.16.0.md`
- Implementation notes reviewed: yes — `tasks/notes/20260820-0356-release-0-16-0.notes.md`, including the contract right-sizing rationale carried from the 0.15.3 lesson and the `0.15.4` versus `0.16.0` tradeoff table
- Run snapshot: `.ai/harness/runs/run-20260820T044840-15213-20260820-0356-release-0-16-0.json`

## Acceptance Receipt Projection

> **Disposition**: external_pass
> **Reviewer**: Claude
> **Source**: claude-review
> **Actor**: not-applicable
> **Reviewed Subject SHA256**: sha256:ed7b43c54ab4ade52944e421d55422f416797e192e4af23436fa7dd2ba8db4bf
> **Reviewed Subject Scope**: normalized-final-content
> **Reviewed Target Revision**: 37b7cada9f6d9b54d034b0391e0afb25432ab56b
> **Verification Evidence SHA256**: sha256:38106b8276685698059d9cb6fed4f80bb784b9529182ad89774e86f20def5fec
> **Issued At**: 2026-08-19T20:50:25.697Z

- Summary: Release-metadata candidate for 0.16.0 verified against main@37b7cada at branch HEAD 6391e1dd. Gate A diff purity: 15 files +806/-15 with zero product code, under scripts/ only axr7-consumer-e2e.ts REPO_HARNESS_VERSION moves, nothing under src/ or assets/templates/, and every path inside allowed_paths. Gate B anchors: package.json, assets/skill-version.json (version + templateVersion + history entry), the five READMEs, and axr7 all at 0.16.0; every residual 0.15.3 is historical (skill-version history entry, the prior filing, lessons, and this cycle's own plan/contract/notes). Gate C filing evidence independently recomputed rather than trusted: npm baseline 0.15.3 reproduced and npm view repo-harness@0.16.0 returned 404; tarball 502 entries, 10077287 packed, and shasum 76e601adab53a2a93104b70bb4e8b45d81d26924 matched the filing exactly, and a real npm pack tarball carried the identical shasum. The filed unpacked byte count did not reproduce and was corrected before this receipt bound; see the P3 finding. Gate D history clean: three release-prep commits plus the correction commit, all authored by the user, no attribution trailers, no hooks bypass, base and merge-base both main@37b7cada. Gate E CHANGELOG: every 0.16.0 bullet traced to a real commit in v0.15.3..37b7cada (8 commits confirmed by git rev-list --count) with each identifier verified present in src/ - state board --json at src/cli/commands/state.ts:288, BoardDocumentV1 and BoardSliceV1, the bind-time resumed receipt introduced by 6c328b82 at src/cli/commands/sprint.ts:710, the HOOK_HOST exactly-once guard at src/cli/hook/subagent-handler.ts:280, the PreToolUse.edit lease gate in src/cli/hook/mutation-guard.ts, and the finish --merge worktree retirement in 39e359c2; no fabricated entries. Standalone full bun test reproduced at 2702 pass / 1 skip / 1 fail / 20536 expect across 199 files in 610s, where the single failure was reviewer-induced I/O contention and the file passed 6/6 in 3.62s isolated, so the filing's recorded 2703/1/0 stands. bun run check:type exit 0. Contract exit criteria 9/0 Fulfilled. Skill eval evidence honestly recorded as unavailable rather than substituted. Oracles: deterministic_test satisfied by the recorded gate runs, runtime_readback declared pending the publication step. Acceptance covers the candidate only; publish, tag, GitHub Release, and merge to main are unperformed and remain the orchestrator's decision.
- Findings: P3: Filing evidence error found by this gate and corrected before the receipt bound. deploy/release-checklists/260820-repo-harness-0.16.0.md:122 recorded 15,809,024 bytes unpacked, which reproduced under none of three independent measurements: npm pack --dry-run --json unpackedSize 15,425,182 (deterministic across two runs on a clean tree), per-file stat sum over the 502 files of the extracted real tarball 15,425,182, and du block-rounded 16,515,072. The cross-version trend corroborated the error, with 0.15.2 at 15,076,890 and 0.15.3 at 15,309,205 against a filed +500K jump while packed size grew only 36K. The gate returned FAIL rather than bind an external_pass receipt over an unreproducible number in a durable release record; the orchestrator dispatched the fix and commit 6391e1dd corrected the line to 15,425,182 and ledgered the flake observation, which this gate then re-verified against a fresh npm pack. All other tarball figures matched the filing exactly throughout: 502 entries, 10,077,287 packed, shasum 76e601adab53a2a93104b70bb4e8b45d81d26924.; P3: A third member of the 5000ms load-sensitivity timeout class surfaced during this review. tests/runtime-evidence-receipt.test.ts (CLI writes the separate receipt only after all three runtime invariants pass) timed out at 5000ms with 8503ms actual during the reviewer's full-suite run, while the reviewer was concurrently running npm pack and tar extraction against the same disk, and passed 6/6 in 3.62s in isolation. The failure is reviewer-induced I/O contention, not a regression in this candidate, and the filing's recorded 2703 pass / 1 skip / 0 fail stands. Appended to the tasks/todos.md flake ledger row by 6391e1dd; the pre-existing pair's third-occurrence root-cause trigger is unchanged.; P2: Architecture gate sandbox collision, pre-existing on main and not reachable from this candidate's diff. scrubHarnessEnv() strips REPO_HARNESS_NODE_BIN by design (whole-prefix) while archctx-provider's resolveCompatibleNodeRuntime() falls back to a PATH-only scan, so check-architecture-sync.sh, and check:release transitively via check-ci.sh:66, cannot pass inside the bounded verifier on machines whose Node >=24 <26 lives only in nvm. The contract's commands_succeed was right-sized at candidate preparation for exactly this reason, matching the 0.15.2 and 0.15.3 release contracts, with out-of-sandbox exit-0 evidence recorded in the filing. Already ledgered in tasks/todos.md. This gate deliberately did not independently re-run check-architecture-sync.sh: doing so advances the architecture drift cursor and would consume the reproducibility of the filing's recorded changed_capabilities=3, which falls outside a review dispatch's zero-side-effect boundary.


## Behavior Diff Notes

- No behavior diff. The candidate changes no product code: `git diff --name-only main...HEAD` matches nothing under `src/` or `assets/templates/`, and its single `scripts/` hit is the `REPO_HARNESS_VERSION` constant in `scripts/axr7-consumer-e2e.ts`. The shipped implementation range was already merged on `main` at `37b7cada`; this candidate only makes the local audit trail agree with it.
- The docs-only correction in `6391e1dd` is likewise not packaged: the tarball shasum and its 502-entry count are byte-identical before and after that commit, confirming `deploy/release-checklists/` and `tasks/todos.md` are outside the published artifact.

## Residual Risks / Follow-ups

- Architecture projection gate is machine-fragile inside the bounded verifier (P2 on the receipt). `scrubHarnessEnv()` strips `REPO_HARNESS_NODE_BIN` while `resolveCompatibleNodeRuntime()` falls back to a PATH-only scan, so any gate reaching archctx fails closed where Node >=24 <26 lives only in nvm. Ledgered in `tasks/todos.md`; the fix belongs to the next work package touching `src/effects/architecture/archctx-provider.ts` or the bounded-verifier environment contract.
- `check-architecture-sync.sh` was not independently re-run by this gate. Re-running it advances the architecture drift cursor and would consume the reproducibility of the filing's recorded `changed_capabilities=3 blocking=0`, which falls outside a review dispatch's zero-side-effect boundary. The filing's out-of-sandbox exit-0 evidence stands unchallenged but unreplicated.
- `runtime_readback` is a declared-pending oracle. It is satisfied only by the post-publication readback, which is out of this contract's scope; the filing's Required Release Sequence still carries publish, tag, GitHub Release, `check-release-published.sh`, and the installed-runtime verification as unchecked items.
- Skill eval evidence is unavailable this cycle and is recorded as unavailable rather than substituted. This candidate's authority rests on the deterministic gates plus the full test run.
- The 5000ms-timeout load-sensitivity class now has three known members. The ledger row in `tasks/todos.md` carries the third-occurrence root-cause trigger; this cycle's instance was reviewer-induced and does not itself advance that counter.

## Scorecard

| Dimension | Score | Notes |
|-----------|-------|-------|
| Functionality | n/a | This gate returns PASS/FAIL against the contract, not a score. Exit criteria 9/0 Fulfilled; there is no functional surface to score because the candidate changes no product code |
| Product depth | n/a | Scope held to release metadata and workflow artifacts; every changed path traces to the stated goal and `allowed_paths_check.outside` is empty |
| Design quality | n/a | The load-bearing decision is version selection, and `0.16.0` over `0.15.4` is justified and recorded: this range completes the kanban program with WP2 board plus WP3 hook, growing the consumer surface by a CLI verb and two hook behaviors |
| Code quality | n/a | No product code in the diff; no new dependency, no compatibility fallback, and no unknown identifier introduced. Version anchors verified mutually consistent down to the packed tarball shasum |

## Failing Items

- None outstanding. One blocking finding was raised and closed during this gate: the filing's unpacked byte count (15809024) reconciled with no measurement method and was corrected to 15425182 by `6391e1dd` before the receipt bound.

## Retest Steps

- Re-run: `repo-harness run verify-sprint --prepare-acceptance` (expect `total=9 failed=0 status=Fulfilled`), then `repo-harness run acceptance-receipt verify --contract tasks/contracts/20260820-0356-release-0-16-0.contract.md`.
- Re-check: `npm pack --dry-run --json` still reports 502 entries, shasum `76e601adab53a2a93104b70bb4e8b45d81d26924`, 10077287 packed and 15425182 unpacked; `rg -n "0\.15\.3" package.json assets/skill-version.json README*.md scripts/axr7-consumer-e2e.ts` still returns only the historical history entry. Run `bash scripts/check-architecture-sync.sh` outside the bounded verifier, not inside it.

## Summary

- PASS. The 0.16.0 release candidate is release metadata only and internally consistent: version anchors, the CHANGELOG section, the filing, and the packed tarball all resolve to one release, and every number the filing asserts was independently recomputed rather than taken on trust.
- The recompute caught a real defect. The filing's unpacked byte count reproduced under none of three measurement methods, and the cross-version trend corroborated it; the gate returned FAIL rather than bind an `external_pass` receipt over an unreproducible number in a durable release record. `6391e1dd` corrected it, and the corrected value was re-verified against a fresh `npm pack` before this receipt bound.
- Everything else in the filing is honest under audit. The tarball's 502 entries, shasum `76e601adab53a2a93104b70bb4e8b45d81d26924`, and packed byte count reproduced exactly; the npm baseline and the 0.16.0 404 reproduced; the full suite reproduced at the filed counts once the reviewer-induced timeout is discounted; and the missing skill-eval evidence is declared unavailable instead of being substituted.
- Every CHANGELOG bullet is backed by a real commit and a real identifier in `src/`, checked by grep rather than read from prose. The eight-commit range, the three feature commits, and the claimed symbols all reconcile.
- Acceptance covers the candidate only. Publication, tag, GitHub Release, and merge to `main` are unperformed and remain the orchestrator's decision.
