> **Archived**: 2026-08-03 11:29
> **Related Plan**: plans/archive/plan-20260803-1053-release-0-12-3.md
> **Outcome**: Completed
> **Lifecycle**: contract
> **Parent Run ID**: run-20260803-1129

# Task Contract: release-0-12-3

> **Status**: Fulfilled
> **Plan**: plans/plan-20260803-1053-release-0-12-3.md
> **Task Profile**: code-change
> <!-- legal values: code-change | docs-only | ledger-closeout | migration | eval-only | delegated-run | bugfix (omit for legacy passthrough); see docs/reference-configs/sprint-contracts.md -->
> **Owner**: kito
> **Capability ID**: root
> **Last Updated**: 2026-08-03 10:56
> **Review File**: `tasks/reviews/20260803-1053-release-0-12-3.review.md`
> **Notes File**: `tasks/notes/20260803-1053-release-0-12-3.notes.md`
> **Exemplar**: `docs/reference-configs/contract-brief-example.md`

## Why

The MainLoopDispatchGuard merged at b039a99a only enforces once the globally installed package carries it; the operator chose release 0.12.3 + global update as the activation path. A wrong release ships inconsistent version surfaces or an unverified package.

## Goal

All 0.12.3 release surfaces prepared exactly per the 2b266f38 (0.12.2) template: version bumps (package.json, .claude/.skill-version, assets/skill-version.json incl. history entry, five README variants), docs/CHANGELOG.md entry, deploy/release-checklists/260803-repo-harness-0.12.3.md filing, tasks/current.md refreshed via refresh-current-status (reason release-0.12.3-prep), with `bun run check:release` passing on the completed tree. Publish/tag/GitHub-release are orchestrator terminal actions outside this contract.

## Scope

- In scope: the eleven release surfaces named in Goal; nothing else.
- Out of scope: src/, tests/, scripts/, assets/templates/, policy files, npm publish, git push, tagging, GitHub release, global package update.
- Taste constraints: <!-- advisory only, no run gate; default style/taste lives in AGENTS.md and the minimal-change policy, use this to record a per-task override -->

## Stop Conditions

- Stop and hand back to the parent if the change would require editing a path outside Allowed Paths.
- Stop if an Exit Criteria command cannot be run in this environment.
- Stop if Goal, Scope, or Exit Criteria are internally contradictory.

## Falsifier

If `bun run check:release` rejects the prepared tree (version drift across surfaces, malformed changelog/filing), the preparation is wrong; that check is the cheapest proof point and is itself an exit criterion.

## Root Cause Evidence

Required when Task Profile is `bugfix`; leave as-is otherwise.

- root_cause: one sentence naming file:line/condition (testable, not "a state issue").
- repro: the command or UI path that reproduces the symptom.
- regression_guard: path to a test that fails on the unfixed code and passes after the fix (must also appear under exit_criteria.tests_pass).
- pre_fix_failure_artifact: path to a captured run of regression_guard on the UNFIXED code. Capture with `bun test <regression_guard> > <artifact> 2>&1; echo "PRE_FIX_EXIT=$?" >> <artifact>` (no pipes — pipes swallow the exit status). The gate requires a non-zero `PRE_FIX_EXIT=` line plus the regression_guard path string in the artifact (see the Root Cause Evidence Gate section in docs/reference-configs/sprint-contracts.md).

## Workflow Inventory

- Source plan: `plans/plan-20260803-1053-release-0-12-3.md`
- Deferred-goal ledger: `tasks/todos.md`
- Review file: `tasks/reviews/20260803-1053-release-0-12-3.review.md`
- Notes file: `tasks/notes/20260803-1053-release-0-12-3.notes.md`
- Checks file: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope gate: edit only paths listed under `allowed_paths`; update this contract before widening scope.
- Completion gate: run `verify-sprint --prepare-acceptance`, record one typed AcceptanceReceipt under the frozen policy below, then run `verify-sprint`; review Markdown is projection only.

## Acceptance Policy

```json
{"protocol":1,"reviewer":"Claude","user_waiver":"allowed"}
```

## Allowed Paths

```yaml
allowed_paths:
  - docs/spec.md
  - plans/
  - tasks/todos.md
  - tasks/contracts/20260803-1053-release-0-12-3.contract.md
  - tasks/reviews/20260803-1053-release-0-12-3.review.md
  - tasks/notes/20260803-1053-release-0-12-3.notes.md
  - .ai/context/capabilities.json
  - .claude/templates/
  - .claude/.skill-version
  - assets/skill-version.json
  - package.json
  - README.md
  - README.es.md
  - README.fr.md
  - README.ja.md
  - README.zh-CN.md
  - docs/CHANGELOG.md
  - deploy/release-checklists/260803-repo-harness-0.12.3.md
  - tasks/current.md
```

## Evidence Requirements

```yaml
evidence_requirements:
  # Set benchmark to required when this contract consumes the harness profile benchmark matrix.
  benchmark: not_applicable
```

## Delegation Contract

```yaml
delegation:
  budget:
    tokens: null
    runner_invocations: null
    wall_time_minutes: null
  permission_scope:
    mode: inherit_allowed_paths
    writable_paths: []
    network: inherited
  roles:
    parent:
      mode: narrate_and_gatekeep
      purpose: approval_checkpoint_owner
    explorer:
      mode: read_only
      purpose: codebase_research
    worker:
      mode: edit_within_allowed_paths
      purpose: implementation
    verifier:
      mode: read_only
      purpose: exit_criteria_review
  runner:
    preferred:
      - subagent
      - codex-exec
      - main-thread
    fallback: main-thread
    brief_is_authoritative: true
```

## Exit Criteria (Machine Verifiable)

```yaml
exit_criteria:
  files_exist:
    - docs/spec.md
    - docs/CHANGELOG.md
    - deploy/release-checklists/260803-repo-harness-0.12.3.md
  artifacts_exist:
    - .ai/harness/checks/latest.json
    - tasks/notes/20260803-1053-release-0-12-3.notes.md
  tests_pass:
    - path: tests/mutation-guard.test.ts
  commands_succeed:
    - bun run check:release
```

## Acceptance Notes (Human Review)

- Functional behavior:
- Edge cases:
- Regression risks:

## Rollback Point

- Commit / checkpoint: base `c8ffee7b`, branch `codex/release-0-12-3`.
- Revert strategy: pre-merge, drop the branch/worktree; post-merge pre-publish, revert the release commit on main; post-publish, `npm deprecate repo-harness@0.12.3` and cut a follow-up patch.
