# BRC audit repair decisions

- Settlement and permission to acquire a new writer remain separate. Nonzero supervised child completion may be charged and recorded as a controller-owned failure while runtime effect inactivity remains unknown.
- Verifier rejection maps successful worker execution outcomes to permanent_failure. The current retry contract has no verification-rejected transient class; automatically retrying would invent scheduling authority.
- Read recovery settles the original reservation at its upper bound, keeps the mutation transaction incomplete, and admits one independently metered read per invocation. Low failure limits may exhaust immediately; stop is deliberate and is not reset.
- Cleanup prerequisite attestation is written under topology exclusion before destructive cleanup and binds exact dispatch, work envelope, final, head and persisted terminal evidence. A missing directory alone is never an inactive proof.
- #342 is not complete: no current host producer revokes detached command effects. Report unsupported command inactivity with operator attention; never remove the command fence or BRC6a admission fence.
- Version probes use the existing process-group supervisor with 64 KiB output ceiling. This does not prove containment of detached probe descendants; that acceptance boundary remains unverified/unsupported.

> **Substantive Change SHA256**: `sha256:972bd3227219ed840580f8d1675580044aef332c39bbb6ad1e1ab0bffba5b7e2`

## Approved continuation

User approved shared Git/GitHub read recovery. The historical out-of-scope stop is cleared. Stable logical requests and exact immutable per-generation observation requests now allow changing temporary fetch refs without running new args under old reservations. Snapshot reads replay success; closeout reads explicitly refresh until semantic proof persists.

## Development evidence

Shared-read pre-fix: 0 pass / 4 fail. Fresh-observation pre-fix: 0 pass / 1 fail. New shared-read suite: 16 pass before final fetch-crash addition. Upper snapshot/step/adoption callers: 49 pass. Closeout open/read recovery delta: 9 pass. Cleanup-fetch crash and changed-ref delta: 2 pass. Current canonical acceptance owns the final frozen subject; these are incremental development evidence.

The previous 104 pass / 3 fail run is retained as historical baseline. Its failures were corrected and targeted delta passed. No old failure log is reclassified as a passing final run.
