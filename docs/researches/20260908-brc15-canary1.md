# BRC15 model-free Canary 1

## Scope

This slice integrates accepted BRC14 runtime `13ce5abf` with owner closeout `e71fb76c`. The BRC6a Sprint row remains completed; no additional Connector probe is authorized. Canary 1 uses only fake provider dependencies and disposable repositories. It does not establish real GPT quality, active/manual delivery, or provider-origin exact-revision evidence, and does not close BRC15's Canary 2/3 ladder.

No runtime code, dependencies, new benchmark runner, provider budget or model override is introduced by this slice. The test matrix below points at the existing owners; the contract is the executable verification authority.

## Fault matrix

| Scenario | Executable owner | Required observation |
|---|---|---|
| Ten complete slots / seven valid plus missing tail | `tests/unit/issue-batch-reconcile.test.ts`: distinguishes a ten-slot complete batch from the missing 08..10 tail | Complete only for ten; seven yields exactly 08,09,10 missing |
| Initial authoring disconnect after seven fake Issue writes | `tests/effects/gpt-pro-issue-authoring.test.ts`: Canary 1: initial authoring disconnect after seven fake Issues cannot repeat creation | Intent is durable before fake writes; interrupted initial dispatch stays reserved and retry cannot create another Issue |
| Disconnect after seven observed Issues | `tests/effects/campaign-step.test.ts`: Canary 1: seven of ten Issues survive a disconnected tail request without blind retry | Real observer with fake GitHub runner reads seven; fake GPT tail request names only 08..10, then throws; same and new keys reject with campaign_reconciliation_required before I/O; budget ledger unchanged |
| Duplicate slot | `tests/unit/issue-batch-reconcile.test.ts`: fails closed for duplicate slots, provider availability, incomplete pages and stale main | issue_batch_ambiguous, never choose/close an arbitrary duplicate |
| Malformed metadata | `tests/unit/issue-batch-reconcile.test.ts`: strict metadata fence, invalid slots, designated repair unfilled | Invalid metadata stays invalid; failed designated repair becomes unfilled |
| Issue edit drift | `tests/unit/issue-batch-reconcile.test.ts`: prior body changes / marker change / second edit | issue_source_drift; no silent fresh baseline |
| Controller crash | `tests/effects/campaign-step.test.ts`: crash after heartbeat receipt / interrupted final step receipt | Existing durable result settles without repeating the effect |
| Cleanup crash | `tests/effects/campaign-closeout.test.ts`: worker publication closeout mode=cleanup-crash and cleanup-fetch-crash | Historical accepted fixture exercises exact cleanup recovery and preserves live safety guards |
| Audit wrong SHA | `tests/effects/campaign-fresh-audit.test.ts`: audit outcome wrong-sha never advances; fresh audit acceptance rejection | Observation remains unverified and group acceptance stays blocked |

The initial-dispatch test injects seven fake Issue writes then throws, proving retry does not invoke that provider again. The separate seven-observed-Issue scenario injects a disconnect in the missing-tail followup after a complete observation of the first seven. It proves controller recovery at that effect boundary; it does not claim a real browser disconnected during the seventh Issue write. The fixture only parameterizes authorized slot count, preserving the existing two-slot default.

## Verification and completion accounting

Run the active contract's named reconciliation, heartbeat, closeout, audit and CLI suites, TypeScript, and the six repository integrity checks through prepare-acceptance. Its final run and AcceptanceReceipt identify the accepted subject. No full test suite, real browser, real GitHub write or stopped budget is used.

BRC6a remains owner-closed (Sprint 14/17). BRC14 runtime implementation is accepted separately; its live audit remains pending. BRC15a real shadow observations and BRC15 active/manual Canary 3 remain distinct unfinished work. Passing this matrix closes only the model-free Canary 1 slice.
