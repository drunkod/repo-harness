# BRC browser defaults, GitHub activation and provider observations

## Authority and implementation

Campaign authoring, fill/edit and readback no longer inject model or thinking values. They request GitHub through the existing `chatgptApp` interface. Browser follow-up no longer re-injects an old session's requested model/effort. The current ChatGPT UI selection remains the user's configuration authority.

Oracle candidate `43adb603` in Ancienttwo's local Oracle checkout extends `26e12021` with `--browser-app` and `--write-session`. Its app action uses the observed composer-plus menu, selects the exact connected plugin entry, and checks the real ecosystemMention pill. The selected plugin ID is checked again at send after attachment readiness; missing, ambiguous, disabled or changed selections refuse submission. The metadata records UI app selection separately from model and explicit effort observations.

The wrapper reads an exclusive per-invocation descriptor and exact matching metadata under its controlled Oracle home. Console Session ID text, requested slug, model label and answer text are not provider session identity. Malformed/missing evidence stays unavailable and model.verified remains false. Observation objects preserve the provider's declared evidence semantics; they do not establish exact backend model or resolved Connector revision.

No dependency or service was added. New Oracle modules own the app DOM action and exclusive descriptor export. One repo-harness module owns descriptor/metadata validation for the provider boundary. New tests guard missing authority and submission refusal.

## Observed UI and validation

On 2026-09-08 the parent used CUA in a new Work-profile ChatGPT tab: `+ → GitHub` produced `[data-inline-selection-pill][data-symbol="ecosystemMention"]`, `data-keyword="GitHub"`, and `plugin:connector_76869538009648d5b282a4bb21c3d157`. The UI default displayed 6 Pro. No prompt was sent in this check; it confirms the UI contract, not the candidate's runtime execution.

Separately, BRC6a report commit `6cfac409` records explicit activation in a Profile 13 copy and a successful README content probe at conversation `6a9f0045-c728-83ea-a489-27796265282a`. Its saved activation, local line/blob comparison and visible api_tool request were inspected. This corrects the earlier unactivated unavailable result. Full GitHub response/resolved-commit provenance remains missing; changing transport is deferred.

Oracle focused tests: 190 passed across appSelection, promptComposer, thinkingTime, modelSelection, writeSession and browser/sessionRunner; typecheck, build and targeted lint passed. The wrapper's development run passed 85 tests across oracle-session-evidence, authoring, adoption and browser CLI. Canonical final evidence is generated by the active contract; the development run is not its acceptance receipt.

## Remaining BRC boundaries

BRC6a still requires provider-origin exact-version evidence. BRC14 has no fresh audit producer/consumer or accepted-audit rolling group baseline; generic accept_group is not that receipt. BRC15a awaits a fresh authorized real canary, and BRC15 awaits its model-free matrix and active/manual canary. No new grant, GPT request, GitHub write, global installation or release was performed by this implementation.

Main readiness WIP and the completed alignment package remain separate. This package cannot be merged through dirty-target guards. Its tests do not re-label historical canary failures or mark remaining sprint rows done.
