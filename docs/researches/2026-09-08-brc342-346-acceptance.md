# Actual role and version preparation evidence

The Docker runtime regression loads the repository's actual tracked fast-worker and gatekeeper TOML bytes. It checks the profile digest, model, effort and developer instructions passed into the invocation. The model-free executable performs the gatekeeper Git status and HEAD commands. A worker runs a real Node test and writes prepared evidence; the verifier reads that evidence under a read-only mount and returns the exact verifier response shape. The sequential test consumes both real producer terminals, expires the original deadline, removes all four containers through the existing cleanup operator, and proves the terminal and interruption consumers still read the same retained authority.

The version branch covers both roles: no return, TERM-resistant detached descendant, stdout flood, missing/invalid version, expired deadline and cancellation of a daemon-confirmed running probe. A separate Bun process owns preparation; its parent supplies an independent watchdog. Tests require no workload journal and fresh Docker Running=false/Pid=0 readback. The successful-probe expiry case uses a test-only Docker CLI wrapper to delay the first post-probe API call past the original deadline. The protected probe receipt must still be completed/exit 0, but workload preparation must fail without receiving a reset timeout.

The first integrated execution retained two failed cancellation assertions: a start-intent file did not establish that the daemon had started the namespace. The corrected test waits for daemon Running=true before signalling the controller. This changes the exercised boundary; it does not make start-intent alone a positive inactivity proof. Both corrected cancellation cases and both successful-probe expiry cases passed the targeted development check.

The original #363 cleanup slice already owns exact inactive-container removal with indefinite protected-journal retention. No new cleanup authority or TTL is added here. Its accepted containment execution remains a baseline for its original subject; current source equality plus the changed runtime tests form the delta evidence.

## Evidence boundary

These tests use an executable substituted into the pinned watchdog image and do not invoke a model. They establish role wiring, real OS commands, namespace termination and protected receipt consumption. They do not prove a real Codex provider follows the instructions.

The production requireCampaignActiveAdmission still unconditionally refuses new active work, including when the stored revision observation and ledger validate. The existing real-store refusal test verifies this without mocking the admission function and asserts no external calls, budget mutation or Git mutation. Successful active admission cannot be claimed while this fence remains. On 2026-09-08 the Owner explicitly approved closing #342 and #346 on this completed no-model runtime evidence scope. Positive active admission and live provider acceptance remain assigned to BRC14/BRC15; the active-disabled fence remains unchanged.

## Frozen result

All 24 current runtime cases passed in canonical execution vx-5ca0fe3ea25f44848edf on 50be60d1. The unchanged admission/parser criterion also passed. Metadata-only closeout reuses that same-contract runtime execution with exact source/profile/image comparison. The previous aggregate's 40 passes and two failures remain a failed aggregate, and the four-case development rerun is not relabeled as the canonical run.
